1) Step 2: L2 tasks don’t appear after Apply Smart Selection

What’s happening

The backend already exposes deterministic endpoints that return tasks by component:
POST /api/step2/l3 (merge) and POST /api/step2/l3/bulk (grouped by component). These read the normalized Task_Label (your “L2” display) for the selected deliverable + component(s). 

v2.8.1 prod

 

v2.8.1 (Brad update)

The Smart Select flow applies deliverables/components, but the UI isn’t calling the L3/L2 fetch after the auto‑selection. Result: counts in “Selection Summary” update (you can see 219 tasks in the screenshot), but the small “L2 Tasks” widget stays empty because no component was focused and no /step2/l3 call ran to populate it.

There’s also a minor consistency gap: some endpoints remap blank components to “General”, others don’t. If a selected component is “General,” api_l3_for won’t return anything because it filters on exact component and doesn’t remap to blank rows. 

v2.8.1 (Brad update)

 

v2.8.1 (Brad update)

Fix

UI: after “Apply Smart Selection,” auto‑focus the first selected component and call:

POST /api/step2/l3/bulk with { deliverable, components[] } to fill the L2 widget instantly; also set activeComponent so the right side shows tasks without extra clicks. 

v2.8.1 (Brad update)

Backend hardening (small patch): in api_l3_for//api/l3, treat "General" as an alias for blank component before filtering, matching how hours code remaps blanks → “General”. 

v2.8.1 (Brad update)

 

v2.8.1 (Brad update)

Label: rename the UI header to “L2 Tasks” (the backend key is still l3/Task_Label, but this is display‑only).

Important: When you push to Stage 2’s final structure, don’t send the __ALL__ sentinel for components unless you intend to include every task. If you pass __ALL__, the server deliberately inflates to all task groups for that component. Provide an explicit included_task_groups_map per component instead to send only the AI‑picked L2 tasks. 

v2.8.1 (Brad update)

Payload shape (what to send to Step 3 Build)

{
  "selected_components_map": { "deck_strategy": ["Art Direction","Activation Ideas"] },
  "included_task_groups_map": {
    "deck_strategy": {
      "Art Direction": ["Internal Review","Creative Approval"],
      "Activation Ideas": ["Concept Development","Presentation Prep"]
    }
  }
}


This avoids auto‑inflating “all tasks” and mirrors what the small L2 widget shows. 

v2.8.1 (Brad update)

2) Retainer optionality should appear in Step 2

Brad’s build adds retainer detection & helpers:

Detect/classify retainer vs project from RFP (/api/ai/analyze_project_retainer) and suggestion helpers (/api/pricing/retainer_suggest). Use these to pre‑seed toggles and months on each deliverable. 

v2.8.1 (Brad update)

 

v2.8.1 (Brad update)

When you build, the scenario carries retainer data (retainer: {months}), and deliverable rows are typed as Retainer vs One‑Time, with retainer summaries available for exports. 

v2.8.1 (Brad update)

 

v2.8.1 (Brad update)

UI hook: After deliverables are chosen (and/or after Smart Select), call the retainer analysis endpoint and surface a toggle + months field per deliverable. Persist that into the retainers array you send to Build. 

v2.8.1 (Brad update)

3) Step 3: Pricing table must be editable

Brad’s build includes pricing utilities you can wire to in‑place edits:

Redistribute hours across components for a deliverable (POST /api/pricing/redistribute-hours)—use this when a user edits a deliverable’s total or a component’s hours; the API returns a reasoned reallocation. 

v2.8.1 (Brad update)

Audit pricing to verify calculated prices vs expected (useful after edits). 

v2.8.1 (Brad update)

Retainer tools to compute distributions if a deliverable is monthly. 

v2.8.1 (Brad update)

UI pattern

User edits hours/price inline → call redistribute-hours with current component list.

Update the scenario’s items[*].components[*].hours and recalculated hours_by_role/price, then refresh totals and the table.

4) “Old SOW references / caching” issue

Brad added a session clear endpoint to purge server‑side caches (RFP text + embedding cache). Wire a “Reset session” action to POST /api/clear_session and pass a session_id to avoid sticky state between proposals. 

v2.8.1 (Brad update)