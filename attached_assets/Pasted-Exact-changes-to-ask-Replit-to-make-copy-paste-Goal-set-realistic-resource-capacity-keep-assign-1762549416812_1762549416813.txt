Exact changes to ask Replit to make (copy/paste)

Goal: set realistic resource capacity; keep assignments coherent; preserve 8‑hour day; keep FS links; optionally keep/remove ExtendedAttributes as a toggle.

1) Fix resource capacity (critical)

In the XML writer where Resources are emitted (in your convert_excel_to_mspdi / export function—right after you create each <Resource> and set StandardRate), force:

# After setting Name, Type, StandardRate, etc.
SubElement(resource, "MaxUnits").text = "1"      # 100% capacity
SubElement(resource, "PeakUnits").text = "1"     # 100% peak


This replaces the MaxUnits>100</MaxUnits> / PeakUnits>100</PeakUnits> currently present in the 03:44 PM export. (That “100” value is what’s inflating capacity.) Your current writer already builds <Resource> and sets rates; just add these two lines right there so they override any defaults. You can see the existing resource block context in your code where resources are emitted and StandardRate is set.

Why: In MSPDI, MaxUnits is a ratio, where 1.0 = 100%. Setting 100 makes a single person look 100 FTEs.

2) Lock assignment Units formatting + guardrails (good hygiene)

In the Assignments write‑out (you already compute units = work_min / dur_min for Fixed Duration tasks), emit a clamped, rounded value:

units = 0 if dur_min == 0 else min(1.0, round(work_min / dur_min, 4))
SubElement(assignment, "Units").text = f"{units:.4f}"


This keeps “per‑resource utilization” at or below 100% on a given task and avoids any floating‑point oddities. Your writer currently recomputes Units this way; clamping and consistent formatting makes behavior predictable.

If you sometimes allocate multiple roles to a single task (e.g., 4h Designer + 4h Copywriter on a 1‑day/8h task), your upstream allocator already splits hours per role; each role will land at 0.5 Units and the sum across assignments will be 1.0. If you ever observe the sum across roles > 1.0 (because the role hours exceeded task planned hours), add a simple per‑TaskUID renormalization pass after you’ve built the assignments list.

3) Keep 8‑hour day and FS dependencies as you have them

You’re already emitting MinutesPerDay=480, DurationFormat=7, and per‑task Type=1 (Fixed Duration), which is right for Workfront compatibility.

Your FS (Finish‑to‑Start) links with LinkLag=0 are present; if/when you want SS + lag overlaps, you’ll add Type=2 with the desired LinkLag (in minutes) for those relationships. The current file shows FS links with LinkLag=0.

4) ExtendedAttributes toggle (optional)

Extended attributes shouldn’t by themselves block Workfront import, but if you want a belt‑and‑suspenders fallback, keep the switch you added to omit ExtendedAttributes on export and try importing that variant if Workfront throws a schema error. (You’ve already implemented this path.)