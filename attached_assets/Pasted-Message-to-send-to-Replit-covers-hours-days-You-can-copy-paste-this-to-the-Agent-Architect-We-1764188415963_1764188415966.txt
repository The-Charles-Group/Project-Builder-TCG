Message to send to Replit (covers hours + days)

You can copy-paste this to the Agent/Architect:

We’ve fixed persistence and SCENARIO_STORE, but there are two remaining export mismatches between the app and Workfront:

1) Hours mismatch: 200h in app vs 210h in Workfront

Repro:

In Step 3/4 I set Brand Positioning Strategy to:

200 hours

$42,000 total

6-month Semi-Annual retainer

Step 4 tooltip & card both show 200 hours.

After export/import to Workfront, the Planned Hours column shows 210 Hours.

From the latest MSPDI XML:

The OutlineLevel 3 “component” tasks under Brand Positioning Strategy sum to 200h of Work.

The OutlineLevel 5 “leaf” timeline tasks under them sum to 210h of Work.

The Brand Positioning Strategy parent task itself has Work=0.

Workfront rolls up Planned Hours from the leaf tasks, not the summary rows. So it’s using the 210h leaf total and ignoring the 200h summary total.

Request (hours):

In the timeline/export generator:

Pick a single source of truth: the scenario item’s total_hours (e.g. 200) from Step 3.

When building the leaf/monthly tasks for a deliverable:

Let total_hours = item["total_hours"] (this already matches the card / tooltip).

Compute the current leaf total, e.g.:

leaf_total_hours = sum(task_hours_template)
scale = total_hours / leaf_total_hours if leaf_total_hours else 1.0


Multiply each leaf task’s Work by scale so the sum of all leaf tasks’ Work matches total_hours exactly.

Alternative simpler option:

Put all hours on the parent or component summary tasks and set leaf tasks’ Work=PT0M. That way Workfront’s rollup will match the Step 3 total. But right now we have two conflicting numbers (200 vs 210) at different levels.

2) Duration mismatch: 121 days in app vs ~6 days in Workfront

Repro:

In Step 4, I drag/adjust Brand Positioning Strategy so the tooltip shows:

Start: 2025-11-30

End: 2026-03-15

Duration: 121 days

After export/import to Workfront, the same deliverable shows Duration: 6 Days.

From the XML:

The Brand Positioning Strategy parent task has:

Start = 2025-11-30T08:00:00

Finish = 2026-03-15T17:00:00

but Duration = PT480M (8h = 1 day).

All of its children (OutlineLevel 3 and 5) have Start/Finish dates clustered around early December, in a very short window.

Workfront derives the task’s duration from the actual working child tasks, so it ends up with a few days (e.g. 6 days), not the 121-day span I set in the app.

So the Step 4 Gantt edits (121-day bar) are not actually applied to the underlying exported tasks. The XML still reflects the old short template.

Request (duration / dates):

In the timeline/save + export path:

When I adjust a deliverable’s bar in Step 4 and click Save Changes, please persist:

The chosen start_date and end_date (or duration_days) into the timeline state tied to that scenario item.

In the MSPDI export for each top-level deliverable (e.g. Brand Positioning Strategy):

Set the parent task’s Start and Finish based on that saved Step 4 state.

Set Duration accordingly, e.g.:

duration_days = (end_date - start_date).days
duration_minutes = duration_days * WORK_HOURS_PER_DAY * 60
task.Duration = f"PT{duration_minutes}M"


For child tasks under that deliverable:

Either:

Distribute their Start / Finish across the parent’s date range (so earliest child start = parent start, latest child finish = parent finish), and scale their Duration so the roll-up matches the 121-day window, or

If that’s too complex for now, set their Duration/Work to 0 and rely on the parent’s Start/Finish/Work so Workfront shows the correct duration and hours.

Right now, the app’s timeline UI is correct (121 days, 200 hours), but the exported MSPDI still uses the old short template (all child tasks around early December, 210 hours). Workfront is trusting the children, so the imported project shows 210 hours over ~6 days instead of the 200 hours over 121 days that the scenario actually defines.