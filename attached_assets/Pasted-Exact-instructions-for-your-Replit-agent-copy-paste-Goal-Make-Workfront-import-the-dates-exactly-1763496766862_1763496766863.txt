Exact instructions for your Replit agent (copy/paste)

Goal: Make Workfront import the dates exactly as in your Gantt by removing inconsistencies and adding Workfront‑safe constraints.

1) Normalize every non‑summary task so Finish − Start == Duration

Use MinutesPerDay = 480 from the project calendar. 

St.Regis_Nashville_Branding_Age…

For any task where Finish and Start are correct in your plan, recompute <Duration> as working minutes between them and set it in ISO duration format PT{minutes}M.

Must‑fix examples (put these exact values):

Brand Positioning Strategy (UID 204)
Start 2026-01-05T08:00:00, Finish 2026-01-20T17:00:00 → 16 d × 480 = PT7680M for <Duration>.
(Assignments already show 64h over this window. 

St.Regis_Nashville_Branding_Age…

)

Paid Media Strategy (UID 623) → 15 d × 480 = PT7200M. 

St.Regis_Nashville_Branding_Age…

Campaign Strategy (UID 343) → 10 d × 480 = PT4800M. 

St.Regis_Nashville_Branding_Age…

Creative Strategy / Campaign Plan Deck (UID 3) → 126 d × 480 = PT60480M. 

St.Regis_Nashville_Branding_Age…

For leaf tasks that truly take 1 hour, set <Duration>PT60M</Duration> and keep 1‑hour Start/Finish. Do not leave them as PT960M (2 days) with one‑hour Start/Finish; that contradiction is what triggers Workfront to recalc. See, for example, UIDs 621, 624, 539, 342, 44, etc.

2) Use Workfront‑friendly constraints (ignore MSP “Manual*” flags)

Delete these Microsoft‑only fields everywhere (Workfront ignores them and they confuse round‑tripping):

<ManuallyScheduled>, <ManualStart>, <ManualFinish>, <ManualDuration>, <ManualWork>

For tasks whose start date must hold, keep a single start constraint:

<ConstraintType>2</ConstraintType>         <!-- Start No Earlier Than -->
<ConstraintDate>YYYY-MM-DDT08:00:00</ConstraintDate>


You already have this on several tasks (e.g., UID 343, 623, 611); keep it.

Why not “Must Start On”? With predecessors, Workfront can fight MSO and still move things. SNET + a correct Duration is the most stable combination.

3) Lock the finish date without fighting the importer (finish anchors)

Workfront’s MSP importer only honors one constraint per task. To keep exact finishes:

Create a zero‑duration milestone directly after each “anchored” task (same parent), named:
«Task Name» — Finish Anchor

Set:

<Duration>PT0M</Duration>
<Milestone>1</Milestone>
<ConstraintType>3</ConstraintType>      <!-- Must Finish On -->
<Start>YYYY-MM-DDT17:00:00</Start>      <!-- same as finish -->
<Finish>YYYY-MM-DDT17:00:00</Finish>
<ConstraintDate>YYYY-MM-DDT17:00:00</ConstraintDate>


Add a Finish‑to‑Start predecessor from the main task to the anchor:

<PredecessorLink>
  <PredecessorUID>{UID_of_main_task}</PredecessorUID>
  <Type>1</Type>         <!-- FS -->
  <LinkLag>0</LinkLag>
  <LagFormat>7</LagFormat>
</PredecessorLink>


This forces the main task to finish at the required date without needing a second constraint on the same task.

4) Keep task type stable

For every normalized task ensure:

<Type>1</Type>               <!-- Fixed Duration -->
<IsEffortDriven>0</IsEffortDriven>


(Already present on the items shown; retain it so hours don’t change while we correct durations.)