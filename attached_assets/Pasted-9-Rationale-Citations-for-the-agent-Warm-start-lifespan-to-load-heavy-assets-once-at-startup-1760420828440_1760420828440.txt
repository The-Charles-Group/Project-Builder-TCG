9) Rationale & Citations (for the agent)

Warm‑start / lifespan to load heavy assets once at startup (models, indices). FastAPI recommends lifespan events for that pattern. 
FastAPI
+1

Reuse a single httpx.AsyncClient to keep connections alive and reduce TLS overhead; community and docs demonstrate safe reuse patterns. 
Stack Overflow

Run uvicorn with optimized loop & parser (or under Gunicorn worker) for production. 
Uvicorn
+1

Cache Excel → pickle and keep dataframes in memory; Pandas recommends pickle for fast reloads and community patterns support it. 
Pandas
+1

Memoize embeddings (deterministic result for same text) with SQLite or in‑memory cache; guides/FAQs endorse caching to save cost/latency. 
OpenAI Platform
+1

Structured JSON outputs via Responses API with json_schema for correctness, plus streaming for perceived speed. 
OpenAI Platform
+2
OpenAI Platform
+2

Playwright file upload uses setInputFiles(); docs show patterns for inputs and label‑wrappers. 
Runebook
+1

SSE / Streaming patterns are well‑documented for FastAPI; polling is also acceptable and simpler. 
devdojo.com
+1

Frappe Gantt can lag on very large task lists; consider pagination or virtualization. 
GitHub

Load testing using Locust is straightforward; quickstarts and tutorials available. 
docs.locust.io

10) What the Agent should deliver back

The code changes in sections 2.1–2.4 applied and committed.

E2E Playwright test file added and passing against the public app URL or local dev URL.

Optional: Locust file added and a short note of p50/p95 latencies (Fast mode).

A brief summary of SOW alignment for Uncommon Schools (which deliverables were included and final fees/hours).

Appendix: Debug / Tips

If the first “Analyze” after boot is slow, it’s likely the Excel → pickle warm‑up or initial embeddings batch. Subsequent runs should be much faster due to caching.

If OpenAI rate limits occur, add an aiolimiter and backoff retries around client.responses.create(...) and client.embeddings.create(...).

If /api/ai/status/{job} sometimes stalls, add a watchdog to mark jobs FAILED after 8–10 min with a friendly message and partial results if available.