Problem:
Step 3 pricing edits still aren’t used by the MS Project export. The XML is built from the old WBS/task data instead of the merged SCENARIO_STORE scenario.

Evidence (from latest export …11-11AM_EST_c78f4ab7.xml):

In Step 3 I set Brand Positioning Strategy to 100 hours, rate $210, price $21,000, cadence Annual (see screenshot).

In the XML, all assignments whose tasks are in WBS 1.1* (Brand Positioning Strategy subtree) sum to 192 hours and $37,440 (192 × $195).

Across the whole file, assignments sum to 3,012 hours and $587,340, while Step 3 shows 1,040 hours and $204,300.

The number 21000 never appears anywhere in the XML.

What we need the export to do:

Use the merged scenario as the single source of truth

When exporting, read from SCENARIO_STORE[session_id]["scenario"] after merge_scenario_from_sync() for:

planned_hours / Planned_Hours

price_usd / Price_USD

billing_cadence, cadence_units, monthly_price, cadence_price

Don’t pull hours or prices from any pre-merge WBS/timeline cache.

Hours in XML must match Step 3

For each deliverable row in Step 3, the sum of assignment <Work> for that deliverable’s WBS subtree must equal its planned_hours.

Acceptance test: Brand Positioning Strategy → planned_hours = 100 ⇒ sum of Work for WBS 1.1* = 100 hours (not 192).

Price in XML must match Step 3

For each deliverable, total Cost in the XML must equal its price_usd from Step 3 (or 12 × monthly_price for a 12-month retainer).

Implementation options:

Either adjust assignment Work and/or resource rate so that Σ(Work × StandardRate) = price_usd, or

Keep current hours but:

set the deliverable task’s Cost / FixedCost to price_usd, and

zero out resource-driven cost for that deliverable to avoid double-counting.

Acceptance test: Brand Positioning Strategy → price_usd = 21000 ⇒ XML must show a cost of 21000 for that deliverable, and project-level total cost must be 204300.

Cadence / retainer behavior

For billing_cadence == "monthly" and cadence_units == 12:

Create 12 monthly child rows (“Feb assets”, “Mar assets”, …).

Ensure Σ Work over those 12 rows = planned_hours and Σ Cost = price_usd (or 12 × monthly_price).

Regression test

Add a test that:

Builds a scenario with at least one retainer + some one-time deliverables.

Overrides hours and prices in Step 3.

Runs the export builder.

Asserts that:

sum of XML hours == Step 3 total hours

sum of XML cost == Step 3 total price.