Absolutely—here’s a single, drop‑in file you can upload. It will:

Force GPT‑5 only (gpt-5, gpt-5-pro, gpt-5-mini, plus your legacy aliases gpt-5-thinking, gpt-5-thinking-mini).

Block any downgrade (e.g., o1, gpt‑4.x) with a loud, clear error.

Auto‑reroute any GPT‑5 calls made via Chat Completions to the Responses API behind the scenes, so your older code keeps working.

Expose helpers for strict JSON responses and a compute tier knob (mini → low, thinking → medium, pro → high).

Download the file:

👉 sitecustomize.py

How to use (zero code changes in most setups)

Upload sitecustomize.py to your project root.
Python automatically imports a module named sitecustomize on startup if present—so the patch activates without you changing your code. 
Python documentation

That’s it. The file:

Guards all OpenAI calls to allow only GPT‑5 models.

Reroutes GPT‑5 chat.completions → responses.create and adapts the return to look like choices[0].message.content, so your existing code paths continue to work.

Provides two optional helpers:

gpt5_json_schema(client, messages, json_schema, tier="thinking") -> dict

gpt5_text(client, messages, tier="thinking") -> str

If your runtime disables auto‑import for sitecustomize (rare), add one line at the very top of your entry script:
import sitecustomize # ensures the guard patches OpenAI 
Anuradha Chowdhary
+1

What this enforcer does (in plain terms)

Model allow‑list: gpt-5, gpt-5-pro, gpt-5-mini, plus your legacy aliases gpt-5-thinking, gpt-5-thinking-mini. Any other model string raises:
"[GPT‑5 Guard] Blocked non‑GPT‑5 model: <name>"

No silent fallbacks: If an agent tries to “fix” your model to gpt‑4.x or o1, execution fails instead of downgrading.

Responses API first‑class: All GPT‑5 calls use the Responses API (the recommended modern interface), which supports reasoning effort control. 
OpenAI Platform
+2
OpenAI Platform
+2

Chat→Responses compatibility layer: If your code calls client.chat.completions.create(model="gpt-5", ...), the enforcer:

blocks non‑GPT‑5 models,

maps the request into client.responses.create(...), and

returns a tiny adapter that still has choices[0].message.content, so your existing code keeps working.
(Chat Completions is message‑based; the new Responses API is the unified, stateful surface.) 
OpenAI Platform
+1

Compute tier (user‑selectable):
Pass tier="mini" | "thinking" | "pro" to the helpers (or set env AI_TIER), which maps to reasoning.effort = "low" | "medium" | "high". 
OpenAI Platform

Optional: call the helpers directly

If you want strictly‑validated JSON from GPT‑5 without writing extra glue: