Decision

Proceed with Option 1: Implement the native <details> Step-2 executive view (no drawers/overlays, no new JS events).

Remove/neutralize any legacy overlay/drawer code so nothing can block clicks again.

Keep your recent backend feature-flag edits; no further backend changes.

Agent — action list (frontend only)

Implement the simple Step-2 UI

Use the exact <details> HTML + tiny JS I provided.

Call renderExecSimple(scenario) at the end of updatePricingSummary(scenario).

Permanently eliminate overlay blockers

Delete any drawer/backdrop markup you find in Step-2 templates.

Remove/hard-disable CSS rules for overlays:

Look for selectors like .drawer, .backdrop, .overlay, [aria-hidden], z-index: 1000+, position: fixed; inset: 0.

If used elsewhere, scope them away from Step-2 (e.g., .step2 .drawer { display:none !important; }).

Kill any auto-mounted drawer/backdrop JS:

Search for addEventListener('click' targeting “open/close drawer”, or code that toggles .open on a drawer element.

Remove or guard it with if (!document.querySelector('#scope-sections')) return;

Search helpers

rg -n --glob "static/**/*.{js,css,html}" "(drawer|backdrop|overlay|modal)"
rg -n --glob "static/**/*.{css}" "z-index:\s*(9{2,}|1\d{3,})"
rg -n --glob "static/**/*.{js}" "pointerEvents|pointer-events|classList\.add\(.+open"


Cache-bust to avoid stale assets

Append a one-time version to CSS/JS in index.html:

<link rel="stylesheet" href="/static/css/app.css?v=exec-1" />
<script src="/static/js/app.js?v=exec-1"></script>


Ask me to bump ?v= again only if you push further UI changes.

Safety net (in case any overlay remains)

Add this tiny guard at the bottom of app.js (no side-effects elsewhere):

(function(){
  const kill = () => {
    document.querySelectorAll('#drawer-backdrop,.backdrop,.drawer.open').forEach(n=>{
      if (n.id === 'drawer-backdrop') n.setAttribute('hidden','');
      if (n.classList.contains('open')) n.classList.remove('open');
    });
  };
  // Run once on load
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', kill);
  } else {
    kill();
  }
})();


Hide “Total Planned Hours” in Step-2 only

.step2 .total-hours { display: none !important; }

Architect — note (no changes requested)

Your Smart Optimization feature-flag changes in main.py are fine; leave them.

No schema/endpoint changes needed for the Step-2 exec view.

Reference copy/sections remain from the same RFP/scenario data; the Word doc is just content guidance: /mnt/data/SB - L2 to L3 corrected, gantt correct.docx

Acceptance criteria

Step-2 renders Overview + Scope as native <details> sections (expand/collapse works without JS).

No overlay/backdrop elements are present or intercepting clicks on Step-2.

“Total Planned Hours” is hidden on Step-2; back-end totals remain untouched.

Hard refresh (Ctrl+Shift+R) shows updated assets due to cache-buster.