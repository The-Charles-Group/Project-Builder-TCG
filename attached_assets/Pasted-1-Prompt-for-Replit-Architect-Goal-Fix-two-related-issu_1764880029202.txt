1) Prompt for Replit Architect

Goal

 

Fix two related issues in the Agency Project Builder backend (Python main.py) and keep XML / Gantt behavior consistent:

Step 3 cadence / retainer toggling: a deliverable that’s switched back to One‑Time must no longer be treated as a Retainer in Deliverable Pricing Details or in the WBS/XML export.

Step 4 / Workfront Gantt: remove artificial gaps between deliverables so the schedule behaves like a proper waterfall (A → B → C, no idle months unless the user explicitly pushes something out).

A. Files / functions

All work is in the FastAPI backend (single main.py):

build_wbs_with_pricing(...) – the WBS & pricing/timeline builder (large function starting near the comment “PATCH A: Ensure components are inflated for AI picks”).

The deliverable loop inside that function (search for for d in items: and day_cursor = 0).

The XML export builder where we map rows → MSPDI tasks and set OutlineLevel, Summary, CostType, FixedCost, etc.

B. Fix 1 – Correct retainer detection & cadence handling

Problem

 

If a deliverable was once treated as a retainer (months > 0 / is_retainer true) and the user later flips it back to One‑Time in Step 3, the backend still infers it as a retainer because it only checks months > 0 or is_retainer.

 

Target behavior

A deliverable is treated as Retainer only when:

billing_cadence is one of monthly | quarterly | semi_annual | annual

AND months > 0 (derived from cadence/units or explicit retainer settings).

When billing_cadence == "one_time", even if stale retainer_months or is_retainer flags exist, this deliverable should be treated as One‑Time.

Implementation details

 

Inside the deliverable loop in build_wbs_with_pricing:

Read cadence/retainer inputs robustly

months = int((d.get("retainer") or {}).get("months", 0) or d.get("retainer_months", 0) or 0)

billing_cadence = (d.get("billing_cadence") or "one_time").lower()
cadence_units = int(d.get("cadence_units") or 1)


Derive months from cadence when needed

if billing_cadence in ("monthly", "quarterly", "semi_annual", "annual") and months == 0:
    months_per_cadence = {
        "monthly": 1,
        "quarterly": 3,
        "semi_annual": 6,
        "annual": 12,
    }.get(billing_cadence, 0)
    months = months_per_cadence * cadence_units


Guard against stale retainer flags

If billing_cadence == "one_time", explicitly zero out retainer flags on the server side:

if billing_cadence == "one_time":
    months = 0
    d["retainer_months"] = 0
    d["is_retainer"] = False


Then compute:

is_retainer = months > 0 or bool(d.get("is_retainer", False))
deliverable_type = "Retainer" if is_retainer else "One-Time"


This block already exists in partial form; update it so “one_time” cadence forcibly clears retainer months instead of just trusting old data.

Front‑end note (for Step 3 UI, not Python)

When the user sets cadence back to One‑Time, the Step 3 component should also clear:

is_retainer

retainer_months

monthly_hours

Architect should ensure the front‑end sends this in the scenario payload so the backend logic above and the UI stay in sync.

C. Fix 2 – Display Hours instead of Months in “Deliverable Pricing Details”

Goal

 

In Step 3’s “Deliverable Pricing Details” table, we want the numeric column currently labeled “Months” (coming from retainer metadata) to instead show total hours for each deliverable (the same hours you set in Step 3 sliders / fields).

 

What Architect should specify

Rename the column header from Months → Hours.

Data source for that column: use the same field that Step 3 uses for total planned hours per deliverable, e.g. item.hours or item.planned_hours from the scenario payload (not retainer_months).

For retainers, that column should still show total project hours (not monthly hours × months again — that is already handled in the retainer logic). The backend already treats parent_hours_display as the total hours for the deliverable, not “per month × months”; reusing that field in the UI keeps everything consistent.

D. Fix 3 – Remove big gaps between deliverables in Gantt / Workfront

Problem

 

The exported schedule has long idle periods between deliverables (e.g., Brand Positioning ends in late Dec but Paid Media doesn’t start until mid‑Feb), even though a PM would expect the deliverables to form a mostly continuous waterfall.

 

Root causes & intended behavior

Each deliverable has an internal schedule (task groups with duration_days). We should:

Build one cycle duration by summing those durations once.

Use that as both:

Internal cadence for repeated months (for retainers), and

The length we advance the global cursor by when placing the next deliverable.

Deliverables should normally be placed back‑to‑back using a day_cursor, unless the user has explicitly overridden dates via Step 4 (Start_Offset_Days or Start_Date).

XML export should treat top‑level deliverables as summary tasks with empty Duration_Days, letting Workfront recalc summary durations from children, while still keeping them at OutlineLevel 2 (child of Project Summary).

Concrete backend changes (inside build_wbs_with_pricing)

Build schedule offsets once

 

In the deliverable loop:

schedule = d.get("schedule", [])
if not schedule:
    # fall back to DB.build_schedule(...)
    ...
tg_order = sorted(included, key=lambda tg: order_map.get(tg, 999))
duration_by_tg = {str(t["task_group"]): int(t["duration_days"]) for t in schedule}
offset_by_tg = {}
run = 0
for tg in tg_order:
    offset_by_tg[tg] = run
    run += int(duration_by_tg.get(tg, 1))
total_deliv_duration = run  # one-cycle length in working days


(This is already present; make sure total_deliv_duration is not multiplied by months or any cadence factor.)

Respect manual Step 4 overrides

 

Right after computing total_deliv_duration, compute dstart like this:

dstart = day_cursor  # default sequential start

try:
    # If Step 4 stored a start offset, use it
    if str(d.get("Start_Offset_Days", "")).strip() != "":
        dstart = int(float(d["Start_Offset_Days"]))
    # Else if we have explicit Start_Date + project_start, derive offset
    elif d.get("Start_Date") and scenario.get("project_start"):
        import datetime
        ps = datetime.date.fromisoformat(str(scenario["project_start"])[:10])
        sd = datetime.date.fromisoformat(str(d["Start_Date"])[:10])
        dstart = (sd - ps).days
except Exception:
    # fall back to sequential
    pass


This is the “FIX B” block in the doc; ensure it’s present and used for all deliverable types.

Components & tasks – contiguous months for retainers

 

In the component & task loops under each deliverable:

Treat parent_hours_display as the total hours for the entire retainer; base_comp_hours_display splits that total across components. Do not multiply by months again.

For monthly retainers:

Only run the task loop once with full hours (is_monthly_retainer logic).

Use base_offset to place each month’s tasks relative to dstart:

base_offset = dstart + offset_by_tg[tg] + ((month_idx - 1) * total_deliv_duration)


so each cadence iteration is exactly one total_deliv_duration after the previous one — no extra “hidden gap” between cycles.

Advance global cursor correctly

 

At the end of the deliverable loop (just before moving to the next d):

# Advance cursor based on actual start used (preserves ordering for next deliverable)
day_cursor = max(day_cursor, dstart) + total_deliv_duration
prev_deliv_wbs = wbs_deliv


This makes the next deliverable start immediately after the previous one (unless its own Start_Offset_Days or Start_Date pushes it further out), which is what a PM expects from a waterfall plan.

XML export – proper summary tasks & cost type

 

In the XML exporter:

Keep deliverables at OutlineLevel = 2 and mark them as Summary while not also treating them as leaf tasks.

Leave deliverable Duration_Days empty in the CSV/WBS rows so MSPDI/Workfront derives it from children.

Use the helper apply_costtype_and_fixedcost(...) so that:

If FixedCost > 0 on the deliverable row, export it as CostType = 2 with FixedCostAccrual = 3 (end of task).

Child tasks and roles have FixedCost = 0 and Rate_USD/Price_USD = 0 (cost is only at deliverable level).

E. Acceptance criteria

Architect should define acceptance tests like:

Cadence toggle

In Step 3, set Brand Positioning Strategy to Monthly, 12 months; export → Deliverable Pricing Details & XML show Type = Retainer.

Change it back to One‑Time (cadence = one_time; months cleared) and save.

Rebuild scenario / export → Deliverable Pricing Details now shows Type = One-Time and XML has no retainer markings for that deliverable.

Pricing table

Deliverable Pricing Details shows a column labeled Hours (not Months).

Hours in that column equal the Step 3 total hours for that deliverable (what’s shown in the Step 3 summary at the top).

Gantt waterfall

Generate AI Timeline, then export XML and view in Workfront.

Major deliverables (Brand Positioning, Paid Media, Campaign Strategy, etc.) appear as a continuous sequence; the gap between the end of one and the start of the next is at most a few days of natural float (or exactly what’s defined by Step 4 start offsets), not arbitrary months.

Changing a deliverable’s Start_Offset_Days or Start_Date in Step 4 moves its bar and its children, while the subsequent deliverables continue from the new end of that bar.

2) Prompt for Replit Agent (hands‑on code changes)

You can hand this to the Agent after the Architect lines up the high‑level plan:

Please open main.py in this Replit and implement the following concrete changes:

In build_wbs_with_pricing(...), inside the main for d in items: loop:

Normalize cadence and retainer fields:

Read months from d["retainer"]["months"] or d["retainer_months"] (default 0).

Read billing_cadence = (d.get("billing_cadence") or "one_time").lower() and cadence_units = int(d.get("cadence_units") or 1).

If billing_cadence is one of monthly|quarterly|semi_annual|annual and months == 0, calculate months from cadence (1,3,6,12 × units).

If billing_cadence == "one_time", force months = 0, d["retainer_months"] = 0, d["is_retainer"] = False.

Then set is_retainer = months > 0 or bool(d.get("is_retainer", False)) and deliverable_type = "Retainer" if is_retainer else "One-Time". Make sure the Type field in the deliverable WBS row uses this value.

Still in build_wbs_with_pricing(...), when building the schedule for each deliverable:

Ensure duration_by_tg, offset_by_tg, and total_deliv_duration are computed by walking tg_order once and summing duration_days once (do not multiply by months).

Immediately after computing total_deliv_duration, compute dstart as:

Default dstart = day_cursor.

If Start_Offset_Days is present and non‑blank, set dstart to that integer.

Else if Start_Date and scenario["project_start"] exist, compute day offset between those dates.

Wrap this in try/except and fall back to day_cursor on parse errors.

In the component+task loops:

Confirm the code uses comp_hours_total_display directly as the component’s total hours and does not multiply by months again.

For monthly retainers, verify is_monthly_retainer logic:

is_monthly_retainer = (billing_cadence == "monthly") and months > 0

month_loop_count = 1 if is_monthly_retainer else (months if months else 1)

For monthly retainers, use full component hours for tg_target; for others, use per‑month hours (comp_hours_per_month).

For each task, base_offset must be dstart + offset_by_tg[tg] + ((month_idx - 1) * total_deliv_duration).

At the very end of the deliverable loop, set:

day_cursor = max(day_cursor, dstart) + total_deliv_duration
prev_deliv_wbs = wbs_deliv


In the XML export function:

Use apply_costtype_and_fixedcost(...) on every task to set CostType and FixedCost based on row FixedCost / Price_USD (already defined; just ensure it’s called consistently).

Ensure deliverable rows (OutlineLevel 2) have:

Summary = 1, DurationFormat = 53 (elapsed days + manual), ConstraintType/ConstraintDate as in the existing deliverable branch.

No duplicate Summary/Constraint tags from overlapping branches (root vs deliverable vs summary).

Confirm Work / Duration for the root task is PT0M and that only leaf tasks carry work hours; deliverables carry FixedCost only.

(Frontend hint if you can locate it) In the Step 3 “Deliverable Pricing Details” table:

Rename the Months column to Hours.

Bind it to the same hours field used to compute each deliverable’s price (e.g., item.hours / Planned_Hours), not retainer_months.

After implementing, please:

Run the API on Replit.

Create a scenario matching the St. Regis Nashville sample, generate an AI timeline, and export the XML.

Confirm in the local Gantt view and in the exported XML (by checking Start/Finish dates, and, if possible, importing to Workfront) that deliverables form a continuous waterfall with no extra month‑long gaps unless explicitly defined by Start_Offset/Start_Date overrides.

If you want, I can also help you turn this into a very short “TL;DR” version you can paste into the Architect chat and keep this longer one just for the Agent.

ChatGPT said:

Replit agent PM brain

Yep, your memory is spot‑on – there is a little “PM brain” in main.py that assumes 6.5 productive hours per day, not 8.

 

Here’s where it lives and what it does:

Timeline duration engine (the PM brain)
Inside AgencyDB.hours_to_duration_days(...) you’ve got:

hours_per_day = 6.5 # Realistic productive hours (not 8)

A focus_factor = 0.7 and an available_resources estimate (≈0.5 FTE per role).

Effective daily capacity is calculated as
effective_daily_capacity = hours_per_day * available_resources * focus_factor
and duration is ceil(hours / effective_daily_capacity) in business days.

That’s the “PM brain” Brad/Ross were talking about. It’s used when the system turns Step‑3 hours into Step‑4 durations for task groups and deliverables.

XML export + Workfront side uses 8h/day
Separately, the XML export path defaults to 8.0 hours per day:

hours_per_day = payload.hours_per_day with a default of 8.0 in the XML export endpoint.

Inside convert_excel_to_mspdi, it also defaults to hours_per_day: float = 8.0.

And the DURATION FIX for leaf tasks explicitly does required_days = max(1, ceil(work_hours / 8.0)) and uses 480 minutes per day.

So you’ve got two different assumptions:

Scheduling “brain”: 6.5 h/day (then reduced further by focus factor & FTE assumptions).

MSP/Workfront export: 8 h/day, straight up.

That mismatch doesn’t create gaps between deliverables by itself, but it does stretch the initial durations the PM brain generates compared to an 8‑hour flat assumption, which is part of why things feel longer / more spread out than a naive “hours ÷ 8” calc.

A tiny extra thing:
The resource‑risk calculator also uses a default hours_per_day of 6 if nothing is passed in metadata, which is only for idle‑time cost estimates, not the main Gantt, but it’s another slightly different assumption:

If you want to tell Replit Agent / Architect what to do with this:

Point them specifically to AgencyDB.hours_to_duration_days in main.py.

Explain that right now it hard‑codes hours_per_day = 6.5 and applies a focus factor of 0.7.

Ask them either to:

Align it to 8.0 hours (and maybe keep the focus factor if you still want some realism), or

Make hours_per_day a scenario/UI parameter and thread that same value into:

hours_to_duration_days (PM brain),

calculate_resource_risk (metadata["hours_per_day"] default),

All XML export calls (hours_per_day argument).

That would unify the “brain” with what Workfront / MSP sees and make it much easier to reason about why a 100‑hour block takes N days in the Gantt.