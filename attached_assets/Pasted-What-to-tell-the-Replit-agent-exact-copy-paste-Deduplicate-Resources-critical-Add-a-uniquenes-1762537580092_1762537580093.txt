What to tell the Replit agent (exact, copy‑paste)

Deduplicate Resources (critical)

Add a uniqueness guard when emitting <Resources>:

Keep a set of used UIDs and IDs.

If adding “Unassigned”, allocate a fresh pair (e.g., 1000/1000).

Never reuse UID or ID values already present.

Re‑export and confirm there is exactly one <Resource> with <UID>1</UID> (Copywriter) and that Unassigned—if present—uses a different UID/ID. (Your 12:04 file clearly reuses UID=1 for both; that must be fixed.)

Encode duration in working minutes, not calendar minutes

In the MSPDI writer, change the duration encoder to:

minutes_per_day = 480  # must match <MinutesPerDay>
duration_minutes = int(round(duration_days * minutes_per_day))
task_elem.find('Duration').text = f'PT{duration_minutes}M'


Sanity check: a 1‑day task should write PT480M, not PT1440M. Your file shows PT1440M for 1‑day spans; that’s wrong.

Remove constraints from summary tasks

For tasks with <Summary>1</Summary>, do not write <ConstraintType>/<ConstraintDate>. Use ASAP (ConstraintType=0) on non‑summary tasks if you must specify it.

(Optional but requested) write SS + lag when appropriate

Stop hard‑coding FS+0 for every link. Feed the dependency type and lag from your scheduler into the writer. Look for blocks that currently write:

<PredecessorLink>
  <Type>1</Type>
  <LinkLag>0</LinkLag>
  <LagFormat>7</LagFormat>
</PredecessorLink>


and replace with the computed Type (3 for SS) + positive LinkLag in minutes (days × 480), keeping <LagFormat>7</LagFormat>. The 12:04 file still shows FS+0 in places like UID 8.

(Recommended) emit Assignments so Workfront shows Planned Hours

Ensure you actually write <Assignments> with <Work> per assignment. In the 11:24 AM file, <Assignments /> was empty—hence 0 hours in WF. Populate them from your role‑hour allocations.

Quick triage checklist before re‑importing

Resources: one row per UID/ID, no duplicates; if “Unassigned” exists, it uses a unique UID/ID. (This is your current blocker.)

Calendar: <MinutesPerDay>=480 and durations use PT{days*480}M (spot‑check that 1 day == PT480M).

Summaries: no constraints on summary tasks; parents don’t carry work.

Dependencies: SS+lag where intended (not required to import, but required per your boss).

Assignments: <Assignments> populated so “Pln Hrs” ≠ 0 after import (not required for import to succeed, but important for your view).

Bottom line

The Replit proposal to “skip ExtendedAttributes” is a red herring—the import failure is caused by duplicate Resource UIDs/IDs (Copywriter and Unassigned both use UID=1/ID=1). Fix that first, then correct duration units (480 min/day), remove summary constraints, and add SS+lag/Assignments as needed. After those changes, re‑export and the file should import cleanly into Workfront.