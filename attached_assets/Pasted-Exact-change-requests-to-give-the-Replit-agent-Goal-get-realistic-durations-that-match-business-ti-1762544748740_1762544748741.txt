Exact change requests to give the Replit agent

Goal: get realistic durations that match business time, emit SS + lag where appropriate, and keep assignments consistent so Workfront accepts the file and the Gantt behaves as a seasoned PM would expect.

A) Make duration = business span (no “max”), and compute finish in business time

Replace the “max” when recomputing DurationHours with a direct assign:

# BEFORE
sched["DurationHours"] = max(sched.get("DurationHours", 0), span_min / 60.0)
# AFTER
sched["DurationHours"] = span_min / 60.0


(Found in the duration recompute loop.)

Stop using start_date + timedelta(hours=duration_hours); add a helper to add business minutes using the same BUS_BLOCKS you already defined and set Finish with it:

def add_business_minutes(start_dt, minutes):
    # walk day by day using BUS_BLOCKS [(08:00-12:00),(13:00-17:00)]
    # consume minutes only inside business blocks, skip weekends
    # return end_dt aligned to business time
    ...

# BEFORE
end_date = start_date + datetime.timedelta(hours=duration_hours)
# AFTER
end_date = add_business_minutes(start_date, int(round(sched["DurationHours"]*60)))


(This replaces the calendar-time addition shown here.)

Snap Durations in the XML writer to 480-minute blocks after the above fix (you already do this), but now the snap will match the Start/Finish span:

dur_minutes = ((dur_minutes + 479) // 480) * 480


(Keep as-is.)

B) Emit SS + lag predecessors (and not just FS/0)

Where predecessors are written, stop hard-coding FS/0:

# BEFORE (hard-coded)
SubElement(pred_link, "Type").text = "1"          # FS
SubElement(pred_link, "LinkLag").text = "0"
SubElement(pred_link, "LagFormat").text = "7"     # days


Parse the dependency spec you already have per row (e.g., your WBS rows’ Dependencies strings) and support at least:

FS, SS, FF, SF

optional lags like +1d, +8h, -1d
Example mapping when creating <PredecessorLink>:

TYPE_MAP = {"FS": "1", "SS": "3", "FF": "0", "SF": "2"}  # MSPDI codes
LAG_UNITS = {"h": 60, "d": 480}

kind, lag = parse_dep_token(token)  # e.g., "SS+2d" -> kind="SS", lag=2*480
SubElement(pred_link, "Type").text = TYPE_MAP[kind]
SubElement(pred_link, "LinkLag").text = str(lag)     # minutes
SubElement(pred_link, "LagFormat").text = "7"        # '7' = days in MSPDI, but minutes in LinkLag is accepted by importers


Then apply your rule engine (PM logic) to generate overlaps (e.g., research can start SS+1d after kickoff, creative can start SS+2d after research, reviews often FS+0).
Right now the serializer’s loop only emits PredecessorUID + FS/0; extend it to use parsed tokens. (Same place you currently add <PredecessorUID>.)

Sanity rule: Do not add constraints (ConstraintType) to summary tasks; let links + calendar drive the dates. (Your writer sets constraints on leaves only; keep it that way.)

C) Keep assignments consistent with the corrected durations

In the assignments builder, you compute Units as work_min / dur_min and snap to 480. That’s good—after fixing (A), Units will reflect reality. (Code is already here.)

Ensure Units are numeric strings with a decimal (e.g., 1.0, 0.5)—your current output sometimes shows 1. which most parsers accept, but format it with "{:.4f}".format(units) to be safe.

D) Minor Workfront import hygiene

Keep ExtendedAttributes if you use them, but ensure numeric values are plain numbers (you already output 1950.0, etc.). No need to strip them entirely—just avoid unknown custom FieldIDs unless you rely on them in Workfront. The latest file shows numeric Value fields, which is fine.