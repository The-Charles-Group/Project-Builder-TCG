1. Remove the duplicate Project Scenario block

In the Step 3 template (the HTML/JSX file you just edited), there are two “Project Scenario” sections:

The new one you already moved so it appears right after the config grid ✅

The original one still further down in the file (around line ~543 of the old version) ❌

Action:

Keep the first Project Scenario block (the one right under the config + Build button).

Delete the second Project Scenario block completely (the duplicate card with the same “Project Scenario” heading and table).

If it helps them find it, tell them to search for the heading text:

<h3>Project Scenario</h3>


and make sure only one instance of that whole card remains.

2. Move the Re-build Scenario / Reset from Step 2 buttons

Right now those buttons are still sitting in (or right under) the Deliverable Pricing Details area. We want them to live with the totals.

Search the Step 3 template for the button labels:

Re-build Scenario
Reset from Step 2


Those two buttons likely live in a shared <div> or flex row.

Action:

Cut the entire container that wraps both buttons, e.g.:

<div class="flex justify-end gap-3 ...">
  <button ...>Re-build Scenario</button>
  <button ...>Reset from Step 2</button>
</div>


Paste it below the Grand Total card, so the final order inside Step 3 is:

<!-- Deliverable Pricing Details (read-only table) -->

<section id="summary">
  <!-- One-Time Deliverables card -->
  <!-- Monthly Retainer Services card -->

  <!-- Grand Total card -->

  <!-- ACTION BUTTONS: move here -->
  <div class="flex justify-end gap-3 mt-4">
    <button ...>Re-build Scenario</button>
    <button ...>Reset from Step 2</button>
  </div>
</section>


Don’t change the click handlers – just move the markup.
The buttons should still call the same endpoints they do now; we’re only changing layout.

3. Final desired vertical order (for them to verify)

Tell them to verify that, visually, Step 3 appears in this order:

Config + green Build Scenario banner

Project Scenario card (editable: Hours / Rate / Price / Cadence + Edit buttons)

Deliverable Pricing Details (read-only breakdown tree)

One-Time Deliverables summary card

Monthly Retainer Services summary card

Grand Total card

Re-build Scenario (blue) and Reset from Step 2 (orange) buttons