1) Define a single, canonical column order (add once)

File: main.py (near the top, under other helpers)

# ===== Workfront column order (now includes Service_Department) =====
WF_COLUMNS = [
    "Project_Name", "WBS_ID", "Parent_WBS_ID",
    "Task_Name", "Deliverable", "Component", "Task",
    "Service_Department",            # <-- NEW, own column
    "Role", "Seniority",
    "Planned_Hours", "Start_Offset_Days", "Duration_Days",
    "Dependencies", "Assignee_External_ID", "Notes",
    "Rate_USD", "Price_USD"
]

2) Standardize the DataFrame before writing (CSV/XLSX)

File: main.py (place next to your Excel formatting helper)

def _finalize_wf_df(df: pd.DataFrame) -> pd.DataFrame:
    # Ensure all expected columns exist
    for col in WF_COLUMNS:
        if col not in df.columns:
            df[col] = "" if col not in {"Planned_Hours","Start_Offset_Days","Duration_Days","Rate_USD","Price_USD"} else 0

    # Reindex to canonical order
    df = df[WF_COLUMNS].copy()

    # Enforce numeric types & pricing identity
    df["Planned_Hours"]     = pd.to_numeric(df["Planned_Hours"], errors="coerce").fillna(0).round(0).astype(int)
    df["Start_Offset_Days"] = pd.to_numeric(df["Start_Offset_Days"], errors="coerce").fillna(0).round(0).astype(int)
    df["Duration_Days"]     = pd.to_numeric(df["Duration_Days"], errors="coerce").fillna(0).round(0).astype(int)
    df["Rate_USD"]          = pd.to_numeric(df["Rate_USD"], errors="coerce").fillna(0).round(2)
    df["Price_USD"]         = (df["Planned_Hours"] * df["Rate_USD"]).round(0).astype(int)  # Hours × Rate (whole USD)

    return df


If you already added the _apply_number_formats(ws, df) function for Excel styling, keep it—we’ll call it after writing.

3) Ensure the builder sets Service_Department on every row

You already wired this from v3 → All_Task_Rows (col D) in the previous patch. Keep those three lookups:

svc_deliv = DB.service_department_for_deliverable(...)
svc_comp  = DB.service_department_for_component(...)
svc_task  = DB.service_department_for_task(...)


…and make sure each row dict you append includes "Service_Department": svc_*. If you lost those lines, re‑add exactly those three fields in the Deliverable / Component / Task rows.

(Reason: v2’s original export never created this field at all. )

4) Apply the finalizer everywhere we write files
A) Single‑scenario export (/api/export)

Right before saving:

df = build_wbs_dataframe_from_scenario(payload.scenario, project_name)
df = _finalize_wf_df(df)  # <-- ensure Service_Department column + pricing identity

if fmt == "csv":
    df.to_csv(out_path, index=False)
else:
    with pd.ExcelWriter(out_path, engine="openpyxl") as xw:
        df.to_excel(xw, index=False)
        _apply_number_formats(xw.sheets[list(xw.sheets.keys())[0]], df)

B) Two‑tab workbook (/api/export_workbook)

After you build each sheet:

dfA = build_wbs_dataframe_from_scenario(payload.scenario_a, project)
dfB = build_wbs_dataframe_from_scenario(payload.scenario_b, project)
dfA = _finalize_wf_df(dfA)
dfB = _finalize_wf_df(dfB)

with pd.ExcelWriter(out_name, engine="openpyxl") as xw:
    dfA.to_excel(xw, sheet_name=sheetA, index=False)
    dfB.to_excel(xw, sheet_name=sheetB, index=False)
    _apply_number_formats(xw.sheets[sheetA], dfA)
    _apply_number_formats(xw.sheets[sheetB], dfB)