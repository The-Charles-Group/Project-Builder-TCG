Thanks for the heads‑up. I rebuilt sitecustomize.py from scratch and expanded it—it now includes:

GPT‑5‑only guard (sync + async) with chat→responses adapter.

Strict JSON helpers (gpt5_json_schema, agpt5_json_schema) and text helpers (gpt5_text, agpt5_text) with tier = mini / thinking / pro → effort = low / medium / high.

A background job runner (FastAPI routes) to keep long GPT‑5 analyses off the request thread with bounded concurrency, per‑call timeouts, wall‑time cutoff, SSE progress, plus an optional health route.

A tiny demo analyzer you can call immediately to verify the non‑blocking flow.

Download the updated single file:

👉 sitecustomize.py

This replaces the older file you have. The previous version didn’t include the job runner, async wrappers, native JSON‑schema path, or the health route.

What changed vs. the older file (summary)

Added Async support (_GuardedAsyncOpenAI) so your async code paths get the same GPT‑5 guard and chat→responses mapping.

Added native JSON‑Schema path via text={'format': {'type':'json_schema', ...}} and a fallback to prompt‑injection if the SDK build doesn’t support that shape.

Bundled the background job runner + SSE streaming: non‑blocking long GPT‑5 work; tunable via env (AI_MAX_CONCURRENCY, AI_CALL_TIMEOUT, AI_WALL_TIMEOUT, APB_BATCH_*).

Improved token & effort handling: auto‑maps max_output_tokens from any of (max_output_tokens, max_completion_tokens, max_tokens) and normalizes legacy model ids (gpt-5-thinking*) to canonical (gpt-5, gpt-5-mini) while blocking o1 / gpt‑4.x outright.

Health endpoint helper so Replit’s proxy has a fast readiness check.

How to wire it (two lines + optional)

Drop the file at your project root. (Python auto‑imports sitecustomize; if not, add import sitecustomize once at startup.)

Register routes after you create your FastAPI app:

from sitecustomize import register_job_routes, register_health_route
register_job_routes(app)
register_health_route(app)


Start a background analysis from the frontend:

POST /api/ai/analyze_job
{
  "analyzer": "sitecustomize.demo_analyzer",   // or your own "module.func"
  "tier": "thinking",                          // mini|thinking|pro
  "batch_size": 90,                            // optional; env defaults apply
  "candidates": [{"id":"d1","text":"..."}, {"id":"d2","text":"..."}]
}


Then poll GET /api/ai/jobs/{job_id} or stream GET /api/ai/jobs/{job_id}/stream for progress.

Apply to your codebase (what to change, where)

Based on your own implementation report, here are the surgical edits to stop timeouts and model drift:

ai_planner_agencydb.py

Replace the sequential loop (chunk = 35) with a call to the job runner (batch sizes by tier: mini≈150, thinking≈90, pro≈45). Your current sequential chunking causes requests to run for many minutes and hit proxy timeouts. 

gpt5_implementation_review

Replace manual “schema in prompt + JSON repair” with gpt5_json_schema(...).

main.py

Any openai_client.chat.completions.create(model="gpt-5", ...) now gets rerouted automatically; switch to gpt5_json_schema(...) or gpt5_text(...) for clarity and speed. 

gpt5_implementation_review

ai_timeline_manager.py / ai_pricing_optimizer.py

Calls like client.chat.completions.create(model=os.getenv("AI_REASONING_MODEL","gpt-5-thinking"), ...) should either keep working via the adapter or be replaced with agpt5_json_schema(...) directly. Your current paths include response_format={"type":"json_object"} and temperature—prefer the strict Responses API + JSON Schema path for reliability. 

gpt5_implementation_review

Chunk/Batch strategy

Replace hardcoded chunk = 35 with a tier‑aware env‑tunable batch size and move the long work off the request using the job runner. This prevents the “12% (chunk 2/8)” stall and the “app unreachable” page you saw. 

gpt5_implementation_review

Minimal examples with this file

Strict JSON (sync):

from openai import OpenAI
from sitecustomize import gpt5_json_schema
client = OpenAI()

schema = {"type":"object","properties":{"summary":{"type":"string"},"channels":{"type":"array","items":{"type":"string"}}},"required":["summary","channels"],"additionalProperties":False}
messages = [{"role":"system","content":"Be concise."},{"role":"user","content":"Summarize this RFP and list 3 channels: ..."}]

result = gpt5_json_schema(client, messages, schema, tier="pro")


Text (async):

from openai import AsyncOpenAI
from sitecustomize import agpt5_text
client = AsyncOpenAI()
messages = [{"role":"user","content":"Reply READY"}]
txt = await agpt5_text(client, messages, tier="mini", max_output_tokens=5)

Why this addresses your timeout + model swap issues

No more silent downgrades: Attempted o1 / gpt‑4* calls now raise an immediate, clear error.

Chat→Responses unification: Any GPT‑5 chat call is mapped behind the scenes; legacy code keeps working, while new code can call the helpers directly.

Non‑blocking long work: The job runner frees the HTTP worker immediately and streams progress, so Replit doesn’t show “app unreachable.”

Strict JSON: Eliminates fragile text parsing/repairs that often cause retries and runaway latency.

If you want me to also ship a single‑file front‑end overlay that switches your “Run analysis” flow to the new analyze_job → stream → fetch pattern, say the word and I’ll generate it the same way.

Again, here’s the consolidated file:

Download sitecustomize.py

(Notes derived from your project’s GPT‑5 implementation review, including the chunk = 35 sequential processing and Chat Completions usage.)