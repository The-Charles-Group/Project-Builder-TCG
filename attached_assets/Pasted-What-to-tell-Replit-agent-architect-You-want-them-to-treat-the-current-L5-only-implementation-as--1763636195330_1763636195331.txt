What to tell Replit agent + architect

You want them to treat the current L5-only implementation as half-done: keep the good bits (multi-assignment + aggregation), but fix the dependency builder so it still matches the earlier behavior.

You can paste something like this (or very close) to them:

Bug: L5-only export drops all dependencies

Context:

ENABLE_L5_ONLY_EXPORT=True

L5 aggregation is working (e.g. “Client Review & Revisions” has Work=PT240M and 3 <Assignment>s).

But the exported XML contains no <PredecessorLink> elements at all, so Workfront has no dependencies and compresses the whole schedule.

What I need:

Keep the old dependency logic and apply it on top of the L5 alias map instead of turning it off.

Implementation sketch:

# 1) Build alias map: every original row UID → L5 component UID
#    - L1–L4 summaries: alias = their own UID
#    - L5 components:   alias = their own UID
#    - L6 role rows:    alias = parent L5 UID
uid_alias_map: Dict[int, int]

# 2) Build edges the same way as before, using original UIDs:
edges = collect_all_predecessor_edges_from_scheduler()
# edges: List[(pred_uid, succ_uid, type, lag)]

# 3) Normalize edges through the alias map
normalized = set()
for pred_uid, succ_uid, typ, lag in edges:
    pred = uid_alias_map.get(pred_uid, pred_uid)
    succ = uid_alias_map.get(succ_uid, succ_uid)
    if pred == succ:
        # this is a leaf-to-leaf edge inside the same L5 component – skip it
        continue
    normalized.add((pred, succ, typ, lag))

# 4) Emit one <PredecessorLink> per normalized pair on the successor task


Important rules:

Do not change how the scheduler calculates Start/Finish dates – keep using uid_to_sched as before.

Only collapse edges with the same alias (i.e. inside a component). We still want:

L5 → L5 dependencies between components

L3/L4 → L3/L4 dependencies between phases/deliverables

The dependency filter from the previous “no conga line” implementation was correct; we just need it to run against the alias UIDs instead of silently dropping everything.

Acceptance test with St. Regis:

Under Brand Positioning Strategy → Key Pillars:

L5 components (Client Review & Revisions, Define Pillar Architecture, Internal Review & Critique, Draft Pillars & Proof Points, Brand Narrative, etc.) each appear once.

Each L5 has multiple assignments in Workfront.

Dependencies:

Components still run in the intended sequence (no random overlaps).

The overall duration for Brand Positioning Strategy and the whole project matches the version with ENABLE_L5_ONLY_EXPORT=False.

In the XML:

<PredecessorLink>s exist and point only at L1–L5 UIDs (no removed L6 UIDs).

There are no leaf-to-leaf links inside one L5 component.