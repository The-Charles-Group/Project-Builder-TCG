1. Confirm their data model

👍 The dual-entry store (baseline + scenario) and per-session lock sound good.
Please implement:

SCENARIO_STORE[session_id] = {
  "baseline": {...},   # snapshot from Step 2
  "scenario": {...},   # working copy of Step 3 edits
  "totals": {...},
  "metadata": {...},
}


baseline is only updated when we explicitly rebuild from Step 2.

scenario is what all editing, views, and exports use.

2. Button behavior contract

Green “Build Scenario” (top)

If no baseline yet → build from Step 2, set both baseline and scenario, compute totals, render everything.

If baseline already exists and reset=False → do NOT call Step 2 again; just return current scenario + totals.

Blue “Re-build Scenario” (in summary section)

Use the current scenario only.

Recompute totals and rebuild:

Deliverable Pricing Details table

One-time / Retainer summary cards

Must not change hours/rates/prices in scenario.

Orange “Reset from Step 2”

Restore scenario = deepcopy(baseline) and recompute totals.

Only call Step 2 if baseline is missing.

3. Edit endpoint contract

Add an endpoint for grid edits (e.g. /api/pricing/scenario/item):

Input: session_id, deliverable_id, and patched fields: hours, rate_usd, cadence, price_usd (as applicable).

Behavior:

Update SCENARIO_STORE[session_id]["scenario"].

Run _recompute_totals() and update totals.

Return updated scenario + totals in the response.

Frontend:

Project Scenario edits should send debounced PATCHes to this endpoint.

After a successful response, refresh:

Project Scenario totals row

Summary cards (One-time / Retainer / Grand Total).

4. Export guarantee

For XML/MSPDI export:

Always use:

scenario = SCENARIO_STORE[session_id]["scenario"]
scenario = _recompute_totals(scenario)
wbs_df = build_wbs_with_pricing(scenario)


Never call Step 2 or rebuild baseline inside the export.

Hours/rate/price in Workfront must match what Project Scenario shows at the moment of export.

5. Layout / UX changes

Please reorder Step 3 vertically:

Config + green Build Scenario

Deliverable Pricing Details (read-only)

Project Scenario (editable grid)

Summary + Actions block:

One-Time Deliverables

Monthly Retainer Services

Grand Total

blue Re-build Scenario

orange Reset from Step 2

Re-build Scenario should hit the “regenerate views from working copy” endpoint you described, not Step 2.

6. Acceptance tests (what you’ll actually check)

I’ll consider this done when these flows work:

A. New scenario

Build Scenario → Brand Positioning shows 137h @ 210 = 28,770.

Step 4 tooltip shows 137 hours.

Export XML → Brand Positioning task has Work = 137h and rate 210.

Import into Workfront → Planned Hours = 137, Planned Revenue ≈ 28,770.

B. Edit & rebuild

Change Brand Positioning to 100h in Project Scenario and save.

Summary cards update to 100h and 21,000.

Deliverable Pricing Details updates after Re-build Scenario.

Export → Workfront shows 100h and 21,000.

C. Reset from Step 2

After a bunch of edits, click Reset from Step 2.

Project Scenario, Deliverable Pricing Details, and summary all revert exactly to Step 2 baseline.

Export matches that baseline.