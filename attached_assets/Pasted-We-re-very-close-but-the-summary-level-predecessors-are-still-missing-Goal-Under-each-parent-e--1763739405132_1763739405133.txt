We’re very close, but the summary-level predecessors are still missing.

Goal

Under each parent (e.g. “Brand Positioning Strategy”), the child summaries/components (“Key Pillars”, “Brand Narrative”, “Brand Values Statement…”, etc.) should be chained in order:

Key Pillars → Brand Narrative → Brand Values → Brand Vision → Mission → Purpose

Same idea for other sections (Paid Media Strategy, etc.).

These are L3 tasks (siblings under a L2 parent); in some cases we may also want L2→L2 chaining for big sections.

In Workfront Gantt, this should show as a clear waterfall from top component to bottom, not all starting on the same day.

Current behavior

Service departments are correctly removed (✅).

UID remapping and PredecessorLink writing use wbs_to_new_uid (✅).

But normalized_edges only contains leaf-level dependencies (and we even prune many L5+ ones), so no edges exist between:

Key Pillars → Brand Narrative

Brand Narrative → Brand Values

etc.

The validation block that prints the “brand_chain_map” only checks for these edges; it never adds them.

Change requested (implementation sketch)

Work on the flattened, sorted rows list
After we’ve:

built rows with final WBS (no departments),

sorted them chronologically,
but before building remapped_edges / writing PredecessorLinks.

Build a parent → children map for summary/component rows

# Pseudocode – use the existing rows / uid_to_sched
parent_to_children: dict[str, list[dict]] = {}
for r in rows:
    wbs = r["WBS"]
    if wbs == "1":
        continue  # skip project root
    parent_wbs = ".".join(wbs.split(".")[:-1]) or "1"

    outline_level = wbs.count(".") + 1
    is_summary_like = outline_level in (2, 3)  # L2 deliverables and L3 components
    if is_summary_like:
        parent_to_children.setdefault(parent_wbs, []).append(r)


Within each sibling group, sort and auto-chain

forced_edges = set()

for parent_wbs, children in parent_to_children.items():
    # Sort siblings by Start date (fall back to WBS)
    children_sorted = sorted(
        children,
        key=lambda r: (uid_to_sched[r["UID"]]["Start"], r["WBS"])
    )

    # Build existing edge lookup in WBS space
    existing = set(normalized_edges)  # (pred_wbs, succ_wbs)

    for i in range(len(children_sorted) - 1):
        a = children_sorted[i]
        b = children_sorted[i+1]
        pred_wbs, succ_wbs = a["WBS"], b["WBS"]

        # Only add if there is no edge already
        if (pred_wbs, succ_wbs) not in existing:
            forced_edges.add((pred_wbs, succ_wbs))


Merge forced edges into the normalized dependency list
Right after the validation / brand_chain_map section where you currently do:

normalized_edges = wbs_dependencies
...
valid_normalized_edges = [...]
normalized_edges = valid_normalized_edges


add:

# Inject sibling summary chains (L2/L3) for clean waterfall
for edge in forced_edges:
    if edge not in valid_normalized_edges:
        valid_normalized_edges.append(edge)
        print(f"[CHAIN] ➕ Added sibling dependency {edge[0]} -> {edge[1]}")

normalized_edges = valid_normalized_edges


Leave the rest of the pipeline as-is

Keep the existing filtered_edges logic (no deep L5 chains).

Keep the wbs_to_new_uid remap and remapped_edges.

PredecessorLink writer should automatically emit the new summary-level links because they’re now in normalized_edges.

Acceptance check in Workfront

In the RFP Brief / Casa Dragones projects:

Under Brand Positioning Strategy, expand:

Key Pillars

Brand Narrative

Brand Values Statement and or Proposition

Brand Vision Statement

Mission Statement

Purpose Statement

Confirm each one starts when the previous finishes and the Gantt shows a left-to-right staircase instead of a vertical column.