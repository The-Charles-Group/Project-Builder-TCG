Exact changes to ask the Replit agent for

Below are surgical instructions you can paste to the agent. I’m naming the blocks by what appears in your “Safe build 11.04.25.docx” (same logic as main.py).

1) Fix predecessor types to support SS + lag overlaps

Where: In the “Add PredecessorLinks for dependencies” block (the one that currently sets Type="1").

Change:

Add a function link_for(edge) that returns (type_code, lag_days) using your “seasoned PM” rules (e.g., research can start SS+2d after discovery begins; design may be FS+0 on copy, etc.).

Use MSPDI type codes: 1=FS, 2=SF, 3=SS, 0=FF.

Emit Type="3" for Start‑to‑Start and set a positive LinkLag (in minutes) with LagFormat=7 (days). Example: 2 days lag → LinkLag="960" because 2 × 480 minutes/day.

Concrete patch (pseudocode to drop in where you build <PredecessorLink>):

link_type, lag_days = link_for((pred_wbs, succ_wbs))  # e.g., (3, 2) for SS+2d
SubElement(pred_link, "Type").text = str(link_type)   # 3 = SS
SubElement(pred_link, "CrossProject").text = "0"
SubElement(pred_link, "LinkLag").text = str(int(lag_days * 480))  # minutes/day
SubElement(pred_link, "LagFormat").text = "7"        # days


Also grep the codebase for any other place setting <PredecessorLink><Type> to ensure you’re not writing 2 anywhere. Your file shows Type=2 everywhere, so something else is overriding the FS block.

2) Ensure assignments are emitted (and drive Workfront “Assignments / Actual Hours”)

Where: The “Create assignments” section that currently skips tasks with task_hours <= 0.

Change:

Before the if task_hours <= 0 guard, backfill zero hours from duration so it never skips:

if task_hours <= 0.0001:
    # fallback: assume 1 FTE across the fixed duration
    task_hours = max(task_hours, uid_to_sched[r["UID"]]["DurationHours"])
    uid_to_sched[r["UID"]]["PlannedHours"] = task_hours


Keep your split rules (even/weighted) but always produce at least one assignment (default resource “Unassigned”) so <Assignments> is not empty:

if not role_list:
    role_list = ["Unassigned"]


When writing <Assignment>, you’re already emitting Units, Work, and Cost; keep that, it’s correct for Workfront/MSPDI.

Result to look for in the XML:
A non‑empty <Assignments> section containing entries like:

<Assignment>
  <TaskUID>...</TaskUID>
  <ResourceUID>...</ResourceUID>
  <Start>...</Start>
  <Finish>...</Finish>
  <Units>0.5</Units>
  <Work>PT2400M</Work>
  <Cost>...</Cost>
</Assignment>

3) Make every non‑summary leaf task “Fixed Duration”

Where: The block that populates <Task> properties for non‑summary tasks.

Change:

Right after you compute and write Duration, ensure:

if not is_summary:
    SubElement(task, "Type").text = "1"  # Fixed Duration (not 0)
    SubElement(task, "IsEffortDriven").text = "0"


This makes assignment Units absorb the variance in hours while the Duration stays canonical.

Why: Your XML still shows Type>0 (Fixed Units) for many leaves (e.g., “Internal Review & Critique”). Switching to 1 aligns with Workfront expectations for creative timelines.

4) Update project summary UID logic

Where: The code that recomputes project summary Start/Finish. It checks for UID=="1".

Change:

Detect the project summary as UID=="0" or “the task whose OutlineLevel==0”:

if uid_elem.text in ("0", "1") and task_elem.find("OutlineLevel").text == "0":
    project_task_elem = task_elem


Your current XML clearly uses UID=0 for project summary.

5) Keep the calendar + duration snapping (already good)

You’re setting working days (Mon–Fri, 9–12 & 13–18) and snapping durations to 480‑minute blocks—keep those; they’re correct for Workfront imports.

“Seasoned PM brain” (how to pick overlap + duration)

To satisfy your boss’s note about not stacking everything into 2–4 days and using real logic:

Add a simple rule table in code (deliverable component → dependency template):
e.g., Research → Strategy = SS + 2d; Copy → Design = FS + 0d; Design → QA = FS + 0d; Dev → QA = SS + 1d once Dev is ~20% in.
Feed those rules into the link_for(edge) helper mentioned above so the exporter deterministically emits SS+lag where appropriate (no fixed 2–4 day gaps everywhere).

Derive Duration from hours & likely staffing:
if an item has H hours and you typically staff 1.0 FTE on it, then duration_days = ceil(H / 8). If you expect 0.5 FTE, ceil(H / 4). Write those hours into PlannedHours so assignments have non‑zero work.

(Your writer already recomputes duration from the scheduled Start/Finish and snaps to days; just make sure PlannedHours is populated so assignments appear.)

Bottom line

The file imported to Workfront will show tasks with varied durations and resources available, but no assignees and no overlaps—because Assignments are empty and dependencies are Type=2, Lag=0. Fix the four code areas above and you’ll get: SS+lag overlaps, realistic fixed durations, proper dependencies, and a populated Assignments section that Workfront respects.