Surgical, step‑by‑step fix (won’t overload Replit)

Below are targeted edits you can give Replit. They are limited to the exporter’s resource/assignment emission and a single filter that disables “complete” placeholders.

A) Make Resource UIDs unique, renumber once, and remap assignments

Where your converter builds and writes <Resources> and <Assignments> (this is inside the XML writer used by /api/xml in main.py), enforce a canonical registry by resource name and rename UIDs sequentially once:

Normalize and de‑duplicate by name before writing resources

Build registry = { normalized_name: {...resource fields...} } where normalized_name = role.strip().lower().

After the registry is built, renumber: for idx, rec in enumerate(sorted(registry.values(), key=lambda r: r["Name"].lower()), start=1): rec["UID"] = rec["ID"] = idx.

Write <Resources> from this single, deduped list only

Don’t append to it later; don’t re-iterate another roles list. (The duplicate you’re seeing is consistent with a second write‑pass.)

Rebuild a name_to_uid map after renumbering

name_to_uid[normalized_name] = rec["UID"].

When writing each <Assignment>, resolve ResourceUID via the map

res_uid = name_to_uid.get(normalize(role_name), default_unassigned_uid) and write <ResourceUID>{res_uid}</ResourceUID>.

Add a final validation guard before returning the XML

Ensure len(set(resource_uids)) == len(resource_uids) and every assignment’s ResourceUID is in the resource set. If not, raise and log a clear error.

Where to put this in your code
In your rolled‑back main.py, the flexible exporter is in api_xml_export_flexible and calls the converter that writes MSPDI (same place you compute components/tasks). Add the dedupe/renumber pass just before <Resources> is emitted, and use the validated name_to_uid when writing <Assignments>. (Your current flow builds resources from roles, then writes resources and immediately writes assignments. The small change is to dedupe and then lock the numbering before either block is written.)

Why this is permanent: even if upstream role lists change, this guarantees only one <Resource> per unique role name and one UID per resource, which is exactly what Workfront expects. The 10:33 AM file clearly shows the duplicate UID 6; the steps above eliminate that class of error.

B) Disable auto‑generated “Complete”/phase gate rows

Your earlier XMLs contain deliverable “Complete” and phase gates; your main.py has multiple pathways and flags for milestones. In the rolled‑back file, ensure these are off by default and filtered out before writing <Tasks>:

In the exporter entry point (your /api/xml flexible export), set:

add_milestones=False (or the equivalent flags you use for deliverable milestones/phase gates).

As a belt‑and‑suspenders safeguard, filter tasks by name just before emitting <Tasks>:

Skip any task whose name matches:

endswith(" - COMPLETE")

startswith("Phase ") and "Complete" in name

name.upper().startswith("CLIENT APPROVAL")

This prevents the “phantom” rows from ever appearing in the XML regardless of upstream toggles and stops the artificial extension you saw in Workfront. Prior files show these rows; disabling them at the writer guarantees they won’t creep back.