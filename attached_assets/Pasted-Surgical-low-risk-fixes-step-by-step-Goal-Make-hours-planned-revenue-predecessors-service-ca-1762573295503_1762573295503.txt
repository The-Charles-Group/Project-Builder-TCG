Surgical, low‑risk fixes (step‑by‑step)

Goal: Make hours, planned revenue, predecessors, service categories, and assignments appear consistently across importers, without heavy refactors or runtime cost.

A) Always write Task.Work from assignments (not just from the row’s PlannedHours)

Why: Importers often key off Task.Work. If assignments exist but Task.Work=0, you’ll still see “hrs” blank.

Compute assignment work by task right before writing <Tasks>:

# after you build `assignments`
work_by_task = {}
for a in assignments:
    work_by_task[a["TaskUID"]] = work_by_task.get(a["TaskUID"], 0.0) + float(a["WorkHours"])


When writing each non‑summary leaf task, set Work, RemainingWork, and RegularWork from work_by_task.get(UID, planned_hours):

planned_minutes = int(round(60 * (work_by_task.get(r["UID"], uid_to_sched[r["UID"]]["PlannedHours"] or 0.0))))
SubElement(task, "Work").text           = f"PT{planned_minutes}M"
SubElement(task, "RemainingWork").text  = f"PT{planned_minutes}M"
SubElement(task, "RegularWork").text    = f"PT{planned_minutes}M"


This is just extending the existing write‑path that currently pulls only from uid_to_sched[...]["PlannedHours"].

Effect: Even if the L3 row’s Planned_Hours is blank, Task.Work will reflect the sum of the row’s assignments. Hours now show up reliably as soon as the importer looks at the task grid.

B) Generate assignments even when L3 hours are blank (decouple from merge_identical_children)

Why: Right now, your role‑hour roll‑up into a parent (L3) happens only when merging is ON, but the API always calls with merging OFF.

Add a small fallback aggregator that always runs:

# Build child-hours-by-role for each parent WBS (always, not gated by merge_identical_children)
child_hours_by_parent = {}
for r in rows:
    parent = r.get("ParentWBS")
    if parent and (r.get("PlannedHours") or 0) > 0 and len(r.get("RoleList", [])) == 1:
        role = r["RoleList"][0]
        child_hours_by_parent.setdefault(parent, {}).setdefault(role, 0.0)
        child_hours_by_parent[parent][role] += float(r["PlannedHours"])


Then, when creating assignments, if a leaf task has 0 PlannedHours but child_hours_by_parent has entries for its WBS, emit assignments from those instead of skipping:

role_hours_map = child_hours_by_parent.get(r["WBS"])
if not role_hours_map and (r.get("PlannedHours") or 0) <= 0:
    continue  # truly no hours
for role, hrs in (role_hours_map or {r["RoleStr"]: r.get("PlannedHours", 0)}).items():
    # create assignment for (task UID, role) with WorkHours=hrs


This preserves your non‑merge output structure but guarantees assignments exist when hours live only on role rows. (You already compute Units as Work/Duration on the assignment, so this slots in cleanly.)

C) Make predecessors visible and safe

Why: Some importers drop links on summary tasks or non‑FS relations.

When attaching PredecessorLink, skip summary tasks:

# find successor task_elem ... then:
is_summary = task_elem.findtext("Summary") == "1"
if is_summary:
    continue


Force link type to Finish‑to‑Start unless a specific type is demanded in data:

SubElement(pred_link, "Type").text = "1"  # FS


Your current writer already uses FS in the patched code; add the summary guard to avoid silent drops.

D) Duplicate Service Category into a “safe” built‑in text field

Why: You are writing Text4 “Service Category”, but many views look for “Department” (Text1 alias) by default.

Keep writing Text4 as you do now (so your custom mapping persists).

Also write Text1/Department on each task (don’t overwrite if you already use Text1 elsewhere). That makes the column visible in more default views.

E) Make “Planned Revenue” unmissable

Why: Some tools map revenue differently.

You already write Cost/FixedCost and Revenue (Number3). To maximize visibility:

Continue setting FixedCost and Cost at the task.

Keep Number3 “Revenue” as a custom extended attribute (present today).

If the destination expects revenue at assignment level, mirror Assignment.Cost (you already do) and ensure Task.Cost equals the sum of its assignments (which follows automatically if you compute Task.Work from assignments as in A).

Why this happened (root‑cause summary mapped to code)

Task.Work set from row PlannedHours only → 0 for L3 → “hrs” blank.

Assignments skipped for rows with 0 hours and the roll‑up prealloc code gated behind merge_identical_children=True → few/no assignments for many L3 tasks in your 7:56 PM run (which calls converter with merging False).

Predecessors existed but sometimes attached to summary/odd types → some importers hide them.

Service Category exists as Text4 but not mirrored into a commonly displayed field → looks blank in default grids.

Revenue exists (and Cost/FixedCost exist), but if the destination looks for a different mapping or only rolls up from Work it may appear empty when Task.Work=0.

Exact places to edit (so you can paste into Replit)

convert_excel_to_mspdi – assignment aggregation
Add the “always‑on” child_hours_by_parent aggregation and use it when PlannedHours==0 (see B). Place this in the function where you currently compute assignments (near “Assignments” build loop). Your existing Units/Work/Cost writer for assignments stays the same.

convert_excel_to_mspdi – write Task.Work from assignments
In the Tasks write loop for non‑summary leaves, swap the Work calculation to read the summed work_by_task (see A). Keep your duration logic and “Fixed Duration (Type=1)” as is.

convert_excel_to_mspdi – predecessor guard
In the PredecessorLink section, skip tasks where <Summary>1</Summary> before adding the link; keep Type=1.

Task Extended Attributes
When you add extended attributes to each task, also set Text1/Department along with Text4/Service Category (don’t remove Text4). This mirrors category into a column that many grids show by default. (You already define and write Text4 per task in the 7:56 PM XML.)

Export endpoints
You can keep merge_identical_children=False (it’s correct for preserving detail), because the fix in B ensures assignments exist even when L3 rows have blank hours. The endpoints already pass that flag.