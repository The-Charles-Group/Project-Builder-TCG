Context for the agent (what’s wrong):
The XML is Microsoft Project format. Work values are stored in minutes (e.g., PT3480M) while the project uses an 8‑hour day (MinutesPerDay=480, MinutesPerWeek=2400) and WorkFormat=2 (minutes). Some summary tasks carry huge Work values (e.g., the “Creative Strategy / Campaign Plan Deck” summary shows ~1,696 hours) that don’t match Workfront “Planned Hours,” causing inflated totals when imported. We need to compute summary task Work as the sum of their leaf tasks, and strip cost fields so tools don’t back‑calculate work incorrectly from costs. 

St.Regis_Nashville_Branding_Age…

✅ Prompt to give Replit Agent (copy/paste)

Title: Fix Workfront XML hours (normalize minutes, remove cost back-calcs, correct summary rollups)

Instructions:

Write a Python 3.11 script called fix_workfront_xml_hours.py that:

Accepts --in <path to xml> and --out <path to fixed xml>.

Parses Microsoft Project XML using xml.etree.ElementTree, preserving the MSP namespace ({http://schemas.microsoft.com/project}).

Project‑level sanity:

Ensure <MinutesPerDay> is 480 and <MinutesPerWeek> is 2400. If missing/different, set them.

This matches an 8‑hour day, which is how Workfront planned hours were produced. The input file already shows these values; keep them. 

St.Regis_Nashville_Branding_Age…

Ensure <WorkFormat> is 2 (minutes). If absent, set it to 2. 

St.Regis_Nashville_Branding_Age…

Remove fields that cause back‑calculation of Work:

For every <Task>, delete <Cost> and <FixedCost> elements if present.

Also remove any Revenue/Cost extended attributes (e.g., <ExtendedAttribute> with FieldID used for “Revenue”) so importers don’t recompute hours from dollars. In this file, FieldID 188743715 holds “Revenue”—remove those attributes. 

St.Regis_Nashville_Branding_Age…

Build the task tree and roll up Work correctly:

Build a map of tasks by <UID> and a parent→children map using <WBS> / <OutlineLevel> or by scanning in order.

Define leaf tasks as <Task> where <Summary> is 0 or missing; summary tasks where <Summary> is 1.

For each leaf task, read <Work>; if missing, treat as PT0M. Keep leaf <Work> unchanged.

For each summary task (post‑order traversal), set its <Work> equal to the sum of descendant leaf <Work> minutes and write it back as PT{total_minutes}M.

Example: If a parent currently shows wildly inflated Work (e.g., ~1,696h on “Creative Strategy / Campaign Plan Deck”), overwrite it with the sum of its children so it matches Workfront rollups on import. 

St.Regis_Nashville_Branding_Age…

If <RegularWork> / <RemainingWork> exist on summaries, set them to the same rolled‑up value (or remove them).

Do not touch dates or durations.

Leave <Start>, <Finish>, and <Duration> exactly as in the source. Import tools will recompute durations from dates and calendars; we only want hours fixed.

Logging & validation (print to stdout):

Print total leaf work (minutes and hours) before/after.

For each summary task, print a line: UID, Name, old_work_h, new_work_h, delta_h.

Fail the run (sys.exit(1)) if any leaf task becomes negative or NaN, or if parsing fails.

Output:

Save the result as a well‑formed XML with the same root and namespace.

File name example: append _fixed.xml to the original base name.

Acceptance criteria:

After importing the fixed XML into your tool (MS Project/Smartsheet/etc.) with 8‑hour day settings, total Work equals the sum of leaf tasks, and summary tasks no longer show inflated hours (they equal the sum of their children).

No task’s Work is derived from dollars; cost fields are gone so Work won’t be back‑calculated from the $195/h rates present in the source. 

St.Regis_Nashville_Branding_Age…

(Optional) Feature flag via CLI:
Add --zero-summary-work which sets all summary task <Work> to PT0M instead of rollups (some PMOs prefer tools to compute rollups themselves). Default is rollup.

Why this works (quick rationale you can paste to the agent, too)

The source XML encodes work in minutes (e.g., PT3480M), and the project defines an 8‑hour day (MinutesPerDay=480) with WorkFormat=2, so the correct conversion is already present; the problem is summary tasks carrying non‑rollup work and cost fields that make importers back‑calculate hours. 

St.Regis_Nashville_Branding_Age…

Normalizing summaries to the sum of leaf work and removing Cost/Revenue fields prevents the importer from inflating or recomputing “Work.” The actual leaf “Planned Hours” remain intact and will match Workfront once imported.

How you’ll run it
python fix_workfront_xml_hours.py \
  --in St.Regis_Nashville_Branding_Agency_RFP_10.22.2024_Workfront_Export_Scenario_A_2025-11-13_05-18PM_EST.xml \
  --out St.Regis_Nashville_Branding_Agency_RFP_10.22.2024_fixed.xml