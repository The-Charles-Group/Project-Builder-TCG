✅ What to Implement
1. Fix Price Calculation for Cost

Locate the normalization block after WBS building (likely near where df["Price_USD"] = … appears).

Replace that section with:

# --- Preserve Step 3 prices instead of overwriting ---
df["Planned_Hours"] = pd.to_numeric(df.get("Planned_Hours", 0), errors="coerce").fillna(0)
df["Rate_USD"] = pd.to_numeric(df.get("Rate_USD", 0), errors="coerce").fillna(0)

if "Price_USD" not in df.columns:
    df["Price_USD"] = 0.0

df["Price_USD"] = pd.to_numeric(df["Price_USD"], errors="coerce").fillna(0)

# Only recompute when missing or zero
mask = df["Price_USD"].eq(0) & df["Planned_Hours"].gt(0)
df.loc[mask, "Price_USD"] = (df.loc[mask, "Planned_Hours"] * df.loc[mask, "Rate_USD"]).round(0)


This ensures that any price typed or derived in Step 3 remains untouched.

2. Preserve FixedCost During Row Merge

In convert_excel_to_mspdi() (or whatever helper merges “identical” WBS children), find the loop that merges rows:

for key, group in df.groupby(["WBS_ID"]):
    merged = group.iloc[0].to_dict()
    # ... merges hours, dependencies, etc.


Before writing merged back, add:

# --- Preserve FixedCost values when merging identical children ---
if "FixedCost" in group.columns:
    fixed_cost_sum = group["FixedCost"].fillna(0).sum()
    fixed_cost_max = group["FixedCost"].fillna(0).max()
    # Prefer sum if children are month splits, else max for duplicates
    merged["FixedCost"] = fixed_cost_sum if fixed_cost_sum > 0 else fixed_cost_max
else:
    merged["FixedCost"] = merged.get("FixedCost", 0)


That keeps each deliverable’s FixedCost through to the XML.

3. Ensure Cost Exists on All Rows

Right before XML serialization, ensure these columns exist:

for col in ["Price_USD", "FixedCost"]:
    if col not in df.columns:
        df[col] = 0.0
    df[col] = pd.to_numeric(df[col], errors="coerce").fillna(0)


This guarantees the export has valid numeric values to use.

4. Confirm XML Builder Writes the Correct Costs

Inside the XML creation section (task/assignment export):

# If FixedCost present, prefer it
fixed_cost = float(row.get("FixedCost", 0) or 0)
if fixed_cost > 0:
    task_elem.find("FixedCost").text = f"{fixed_cost:.2f}"
    assignment_elem.find("Cost").text = "0.00"  # prevent double count
else:
    cost = float(row.get("Price_USD", 0) or 0)
    assignment_elem.find("Cost").text = f"{cost:.2f}"
