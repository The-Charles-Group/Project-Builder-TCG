What you should tell the Replit agent + architect
1. Stop using /api/scenarios for Step 3 “Build Scenario”

In the frontend (the code behind the green “Build Scenario” banner button at the top of Step 3):

Find the function that runs when you click Build Scenario. It will currently do something like:

// ❌ current behaviour – this is the problem
await fetch("/api/scenarios", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify(payloadFromStep2),
});


Change it to call the new pricing endpoint instead:

// ✅ new behaviour – uses the working copy in SCENARIO_STORE
await fetch("/api/pricing/build_scenario", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    session_id,                // same session_id you use for Step 4 / exports
    reset: false,              // only true when clicking the orange "Reset from Step 2" button
    selection: {
      deliverable_codes,       // codes selected in Step 2
      components_map,          // selected L2s
      l3_map,                  // selected L3s
    },
    pricing_mode,              // e.g. "Flat_Blended"
    blended_rate,              // 210 or whatever’s in the “Blended rate” field
    rate_band,                 // Nearshore_Value / etc
    project_start,             // ISO date string
    client_budget_usd,
    retainers,                 // retainer config if you’re using it
  }),
});


This endpoint is already implemented in your current main.py (see @app.post("/api/pricing/build_scenario")). When it’s called:

If a baseline exists and reset is false, it does:

scenario = get_working_scenario(session_id)
scenario = _recompute_totals(scenario)
update_working_scenario(session_id, scenario)


…which means it returns exactly the Step-3-edited scenario (137h @ $210, etc).

If reset=True, it calls reset_scenario_from_baseline(session_id) and rebuilds from Step 2 – which is what the orange “Reset from Step 2” button should use.

2. Make sure the orange “Reset from Step 2” uses reset=True

For the Reset from Step 2 button:

await fetch("/api/pricing/build_scenario", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    session_id,
    reset: true,     // 🔴 only this button should ever send true
    selection: { deliverable_codes, components_map, l3_map },
    pricing_mode,
    blended_rate,
    rate_band,
    project_start,
    client_budget_usd,
    retainers,
  }),
});


That matches the backend logic you already have.

3. DO NOT overwrite SCENARIO_STORE directly anywhere else

Ask them to double-check in main.py that:

All Step-3 edit endpoints (the ones hit by the Save button) eventually call:

update_working_scenario(session_id, scenario)


and never do SCENARIO_STORE[session_id] = scenario directly.
The helper functions in main.py are the single source of truth:

set_baseline_and_scenario(session_id, scenario)   # only when building/resetting from Step 2
update_working_scenario(session_id, scenario)     # Step 3 edits
get_working_scenario(session_id)                  # what Step 4 + export should use


If they stick to those three helpers plus the /api/pricing/build_scenario endpoint, you’ll get:

Step 3 Project Scenario: 137h, $28,770

Deliverable Pricing Details after clicking Build Scenario: 137h, $28,770

Step 4 Gantt: 137h

Workfront XML / import: 137h at the right rate

TL;DR to paste to them

Bug: Step 3 “Build Scenario” is still POSTing to /api/scenarios, which rebuilds from Step 2 and ignores my saved Step 3 edits. That’s why hours jump back to 98 even though Step 4 Gantt shows 137.

Fix:

Change the Build Scenario button to call POST /api/pricing/build_scenario with session_id, selection, pricing params, and reset:false.

Change the orange “Reset from Step 2” button to call the same endpoint with reset:true.

Ensure all Step 3 save/edit endpoints use update_working_scenario(session_id, scenario) and never write SCENARIO_STORE[session_id] directly.

The /api/pricing/build_scenario + get_working_scenario/update_working_scenario helpers are already in main.py – the UI just needs to call them.