The UI and SCENARIO_STORE now look correct (137 hours, $28,770, 6-month retainer). But the exported XML / Workfront row shows 822 hours and $172,620, which is exactly 137 * 6 hours and 28,770 * 6 dollars.

So the remaining bug is in the retainer timeline/export calculation, not in Step 3 saving.

Please do the following:

1. Log the scenario item going into the export

In the timeline/export pipeline (where load_export_scenario or the timeline builder loops over scenario["items"]), add a debug log before any hours math:

print(
    "[DEBUG] EXPORT ITEM:",
    item.get("name"),
    "hours=", item.get("total_hours"),
    "months=", item.get("months"),
    "cadence=", item.get("cadence"),
)


For Brand Positioning Strategy in my failing case we expect:

total_hours = 137

months = 6

cadence = whatever value you use for Semi-Annual/retainer

This will confirm that the working scenario is correct before we build the timeline.

2. Fix the hours formula for retainers

In the function that converts a scenario item into monthly tasks (the one that’s creating all the Month 01 – …, Month 02 – … tasks), you currently effectively do something like:

hours_per_period = item["total_hours"]   # ← currently treated as monthly
for m in range(months):
    # build tasks using hours_per_period


So with total_hours = 137 and months = 6, the timeline ends up with:

137 hours per month

137 × 6 = 822 hours total (what we see in the export)

We actually want the opposite:

The Step 3 “hours” value is the total for the whole 6-month retainer, not per-month.

So for retainers, the math should be:

total_hours = item["total_hours"]
months = max(item.get("months", 1), 1)

if item.get("cadence") in ("retainer", "monthly", "quarterly", "semi_annual", "annual"):
    hours_per_period = total_hours / months   # distribute across months
else:
    hours_per_period = total_hours           # one-time deliverables, keep as is


Then build the monthly tasks using hours_per_period, not total_hours.

This should give:

137 total hours over 6 months

≈ 22.83 hours per month in the timeline

Workfront summary row: 137 Hours, cost $28,770, not 822 / 172,620.

3. Verify end-to-end

After updating the math:

Run the same scenario:

6-month retainer

137 hours

$28,770

Export to XML / Workfront.

Confirm:

Project Scenario card still shows Hours = 137, Price = $28,770.

Workfront summary row for Brand Positioning Strategy also shows 137 hours (not 822).

If logs still show hours=137, months=6 going into the export but Workfront shows 822, then there’s still another multiply-by-months somewhere else in the timeline/export path that needs the same treatment.