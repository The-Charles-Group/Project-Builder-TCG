Project: Step-2 Executive View (Accordion + Drawer)
Goals

Make Step 2 skimmable for PMs/Programs/C-level via progressive disclosure.

Keep all current APIs/data models. No backend changes.

Remove “Total Planned Hours” from this panel without breaking anything.

Deliverables

New Step-2 layout:

KPI strip (Price, Duration, Dates).

Overview card (goal, channels/market, 3 highlight bullets).

Scope & Plan sections rendered as Accordion with “View details” → opens a right-side Drawer with tabs.

Feature flag to hide/show “Total Planned Hours”.

Unit + UI tests covering render, expand/collapse, and data mapping.

Data (use existing response the screen already loads)

Map current Step-2 text into six sections:

Brand & Identity

Brand Architecture & Naming

Experiential Activation

Campaign Creative

Content Production

Marketing Collateral & Sales Tools

Program Management & Timeline (ok if this makes it seven; keep each section atomic)

If all bullets arrive as one long blob, split by keyed phrases above. Else, use existing structured fields if already separated.

Component Tree (React—but keep the same idea in your stack)
<Step2>
  <KpiStrip price duration dateRange />         // no hours here
  <OverviewCard highlights goal channels markets />
  <ScopeAccordion>
    <ScopeSection title="Brand & Identity" previewText onExpand={openDrawer}/>
    <ScopeSection title="Experiential Activation" ... />
    ...
  </ScopeAccordion>

  <DetailsDrawer open onClose>
    <Tabs>
      <Tab id="summary">rich text from bullets (paragraphized)</Tab>
      <Tab id="components">table (Component | Hours | Owner | Notes)</Tab>
      <Tab id="risks">short list of risks/assumptions (3–5)</Tab>
    </Tabs>
  </DetailsDrawer>
</Step2>

Minimal component APIs

KpiStrip: { price: number|string, duration: string, dateRange: string }

OverviewCard: { goal: string, highlights: string[], channels: string[], markets: string[] }

ScopeSection: { title: string, previewText: string, fullText: string, components?: Row[], risks?: string[] }

DetailsDrawer: controlled open boolean + onClose()

Interactions

Show only the first 2–3 lines in each ScopeSection (CSS line-clamp).

Clicking “View details” opens DetailsDrawer with Summary | Components | Risks tabs.

Close returns to concise view. Persist which accordion panels are open via local state only.

“Total Planned Hours” removal (UI only)

Do not render the “Total Planned Hours” chip in Step 2.

Add a flag:

const featureFlags = { showHoursKpi: false };


If the chip is reused elsewhere, scope the hide to the Step-2 route/component (or add a CSS utility to hide that node only on this screen).

Styling tokens (tailwind-ish, adapt to your system)

Cards: rounded-2xl shadow-sm p-6 bg-surface

Headings: text-xl font-semibold

Body: text-sm leading-6 text-muted

KPI value: text-2xl font-bold

Chips: px-3 py-1 rounded-full text-xs bg-muted/20

Drawer: width 560–640px, overlay blur-0, z-index above modals

Line clamp: line-clamp-3 on section preview

Accessibility

Accordion buttons are <button> with aria-expanded.

Drawer traps focus; ESC to close; close button labelled.

Tabs use roving tabindex and aria-controls.

Parsing the existing Step-2 text

Source file for reference content (RFP bullets) provided:
/mnt/data/SB - L2 to L3 corrected, gantt correct.docx

If the API already provides structured fields, do not parse text—map directly.

If only one long string is available, split by these anchors (case-insensitive):
Brand Strategy|Brand Identity|Brand Architecture|Experiential Activation|Campaign Creative|Content Production|Marketing Collateral|Program Management

Replit Agent – Tasks

Branch: feat/step2-exec-view

Build KpiStrip, OverviewCard, ScopeAccordion, ScopeSection, DetailsDrawer components with props above.

Wire data mapping from the current Step-2 JSON. Implement simple splitter if needed.

Hide “Total Planned Hours” on Step-2 using the feature flag or scoped CSS.

Add tests:

Renders KPI values and Overview.

Accordion shows preview and expands.

Drawer opens with tabs; tab switches change panel content.

“Total Planned Hours” not present on Step-2.

Lint, typecheck, and ship a storybook/screenshot for each component state.

Solution Architect – Notes

No backend work. Keep recompute of totals as-is; the UI simply doesn’t display hours on Step-2.

Confirm Step-2 API contains (or can derive) price, duration, startDate, endDate, goal, channels, markets, and the scope text.

If “Components/Hours/Owner” exist server-side, surface them in the Drawer’s Components tab. If not, leave that tab hidden for now (feature flag: showComponentsTab=false).

Ensure global hours KPIs in other steps remain unaffected.

Add UX feature flags to your config:

{
  "step2": {
    "showHoursKpi": false,
    "showComponentsTab": false
  }
}

Acceptance Criteria

Step-2 renders KPI strip + Overview + Accordion.

Each section shows a 2–3-line preview; “View details” opens a Drawer with tabs.

“Total Planned Hours” does not appear anywhere on Step-2.

No API/schema changes; all existing calculations still occur server-side.

Lighthouse a11y score ≥ 90 on the Step-2 route.

Nice-to-have (optional)

Persist last-opened section in sessionStorage.

Print-friendly stylesheet for Drawer (so PMs can export a one-pager).