What to tell the Replit Agent (copy/paste)

Goal: ensure every <PredecessorLink><PredecessorUID> points to a leaf task only (Summary=0).

Build a summary lookup after tasks are generated

After you finish writing <Tasks>, walk the task tree and build:

is_summary[uid] = (Summary == 1)

children_by_uid[parent_uid] = [child_uids…] (use OutlineNumber prefix or parent UID if you already compute it)

descendant_leaves_by_summary[summary_uid] = [leaf_uids under that summary]

A leaf is Summary == 0.

Rewrite or drop illegal predecessors before writing them

When emitting each <PredecessorLink>:

pred = predecessor_uid
if is_summary.get(pred, False):
    leaves = descendant_leaves_by_summary.get(pred, [])
    if leaves:
        # Choose a stable, sensible leaf to represent the phase
        # Option A (recommended): leaf with the latest <Finish> date
        pred = max(leaves, key=lambda u: task_finish[u])
    else:
        # No leaves? Then skip this predecessor to avoid invalid reference
        continue
# Now write <PredecessorUID>pred</PredecessorUID>


Do not let any <PredecessorUID> remain pointed at a Summary==1 task.

Add a pre‑export validator (fail‑fast)

Build valid_task_uids = {uid of every Task node}

Count and block the following before writing <Assignments>/<Tasks>:

Any PredecessorUID not in valid_task_uids → drop and log

Any PredecessorUID where is_summary[uid] → rewrite via step 2 or drop and log

Log something like:
"[LINK CLEANUP] Rewrote X summary predecessors; Dropped Y invalid predecessors"

Keep the assignment checks you just added

You already cleaned orphaned assignments. Keep that logic so only leaf tasks receive assignments (double‑check you never emit <Assignment> for Summary=1 tasks).

Quick verification script for the agent

Have them run this right before writing <Tasks> or immediately after building them (pseudo‑code; adapt to your codebase):

# After building tasks list / XML nodes
task_by_uid = {t.uid: t for t in tasks}
is_summary = {t.uid: (t.summary == 1) for t in tasks}

# Gather offenders
illegal_summary_preds = []
missing_preds = []

for t in tasks:
    for link in t.predecessors:  # however you store PredecessorLink
        p = link.predecessor_uid
        if p not in task_by_uid:
            missing_preds.append((t.uid, p))
        elif is_summary[p]:
            illegal_summary_preds.append((t.uid, p))

print(f"[CHECK] predecessors -> missing: {len(missing_preds)}, summary: {len(illegal_summary_preds)}")
# Expect both counts to be 0 before writing XML


On your current 04:08 PM file, this will flag at least the two concrete cases shown above (predecessors → 285 and 291).