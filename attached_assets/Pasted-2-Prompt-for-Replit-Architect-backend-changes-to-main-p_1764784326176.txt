2. Prompt for Replit Architect (backend changes to main.py)

You can copy‑paste this as your instruction to Architect:

Prompt for Replit Architect

We’re working on the Step 4 Gantt timeline in a FastAPI backend (main.py).
I need /api/timeline/save to compute extra metrics so the UI can update the “Resource Risk Management” panel and “DEPARTMENTS” count in real time.

Please do the following in main.py:

Add a helper to compute resource idle‑time risk

Create a function near the timeline code, e.g. just above /api/timeline/save:

def calculate_resource_risk(scen: dict,
                           tasks: list[dict],
                           metadata: dict) -> dict:
    """
    Inspect timeline tasks and estimate idle-time risk per resource.
    Returns a dict with:
      {
        "summary": {
          "high_risk_count": int,
          "total_idle_cost": float
        },
        "items": [
          {
            "resource": str,
            "waiting_days": int,
            "idle_cost": float,
            "risk_level": "Low" | "Medium" | "High",
            "recommendation": str,
          }, ...
        ]
      }
    """
    if not tasks:
        return {"summary": {"high_risk_count": 0, "total_idle_cost": 0.0},
                "items": []}

    hours_per_day = float(metadata.get("hours_per_day", 6))
    # Use scenario blended_rate if available; fall back to DEFAULT_BILLABLE_RATE_USD
    blended_rate = float(scen.get("blended_rate") or DEFAULT_BILLABLE_RATE_USD)
    hourly_rate = blended_rate

    # Build resource -> list[tasks] map
    from datetime import datetime
    from collections import defaultdict

    by_resource: dict[str, list[dict]] = defaultdict(list)
    for t in tasks:
        for r in t.get("resources", []):
            name = r if isinstance(r, str) else r.get("name", "Unknown")
            # Parse dates defensively
            try:
                start = datetime.fromisoformat(t["start"].replace("Z", "+00:00"))
                end = datetime.fromisoformat(t["end"].replace("Z", "+00:00"))
            except Exception:
                continue
            by_resource[name].append({"start": start, "end": end, "task": t})

    items = []
    for resource_name, r_tasks in by_resource.items():
        if not r_tasks:
            continue
        r_tasks.sort(key=lambda x: x["start"])

        total_idle_days = 0
        # Sum gaps between consecutive tasks
        for i in range(len(r_tasks) - 1):
            cur = r_tasks[i]
            nxt = r_tasks[i + 1]
            gap_days = (nxt["start"].date() - cur["end"].date()).days - 1
            if gap_days > 0:
                total_idle_days += gap_days

        if total_idle_days <= 0:
            continue

        idle_hours = total_idle_days * hours_per_day
        idle_cost = idle_hours * hourly_rate

        # Simple tiering logic – adjust thresholds if needed
        if total_idle_days >= 60 or idle_cost >= 50000:
            risk_level = "High"
        elif total_idle_days >= 15 or idle_cost >= 10000:
            risk_level = "Medium"
        else:
            risk_level = "Low"

        recommendation = (
            "Consider reassigning tasks or adjusting timeline to reduce idle time."
            if risk_level in ("Medium", "High")
            else "No immediate action needed."
        )

        items.append({
            "resource": resource_name,
            "waiting_days": int(total_idle_days),
            "idle_cost": round(float(idle_cost), 2),
            "risk_level": risk_level,
            "recommendation": recommendation,
        })

    high_items = [x for x in items if x["risk_level"] == "High"]
    total_idle_cost = sum(x["idle_cost"] for x in high_items)

    return {
        "summary": {
            "high_risk_count": len(high_items),
            "total_idle_cost": round(float(total_idle_cost), 2),
        },
        "items": items,
    }


This should never raise if data is missing; just return empty structures.

Compute departments list and count in /api/timeline/save

In /api/timeline/save, after you calculate total_duration, critical_path, and resource_allocation and before you build the metrics dict:

Build a sorted list of unique departments from the scenario:

Look at scen["items"] and collect non‑empty Service_Department or service_department values.

Example:

departments_set = set()
for item in scen.get("items", []):
    dept = item.get("Service_Department") or item.get("service_department")
    if dept:
        departments_set.add(str(dept))
departments = sorted(departments_set)
department_count = len(departments)


Wire the new metrics into the response

Still inside /api/timeline/save, after resource_allocation is built and before the return:

Call the new helper:

resource_risk = calculate_resource_risk(scen, tasks, metadata)


Extend the metrics dict to include:

"metrics": {
    "total_tasks": len(tasks),
    "total_duration_days": total_duration,
    "critical_path_count": len(critical_path),
    "resource_count": len(resource_allocation),
    "resource_allocation": resource_allocation,
    "departments": departments,
    "departments_count": department_count,
    "resource_risk": resource_risk,
    "saved_at": scen["timeline_last_saved"],
}


Keep all existing keys/behavior intact so current frontend calls don’t break.

Error handling & performance

If tasks is empty, return zeroed metrics and an empty resource_risk.

Do not introduce any heavy I/O inside calculate_resource_risk; it should operate only on the in‑memory scen, tasks, metadata.

This gives the frontend everything it needs to:

Show TOTAL DURATION, TASKS, CRITICAL TASKS, and DEPARTMENTS from metrics.

Populate the “Resource Risk Management” table and summary from metrics.resource_risk.

3. Prompt for Replit Agent (frontend / Gantt wiring)

Now for the UI side. Here’s a prompt you can give Replit Agent (or paste into the code conversation):

Prompt for Replit Agent

We now have /api/timeline/save returning additional timeline metrics:

total_tasks

total_duration_days

critical_path_count

departments

departments_count

resource_risk (summary + items)

The Step 4 screen has:

A Gantt chart for tasks.

A header row with TOTAL DURATION, TASKS, CRITICAL TASKS, DEPARTMENTS.

A “Resource Risk Management” panel (Risk Summary + table).

I want these to update in real time (on mouse‑up) whenever the user moves or resizes Gantt tasks.

Please:

Locate the Step 4 Gantt component

Find the React/JS/TS component that renders:

The Step 4 Gantt chart.

The “Resource Risk Management” section shown in the screenshot.

We’ll call it Step4Timeline below (use whatever file actually exists).

Add state for metrics and resource risk

In that component:

const [timelineMetrics, setTimelineMetrics] = useState<any | null>(null);
const [resourceRisk, setResourceRisk] = useState<any | null>(null);


Helper to call /api/timeline/save

Implement a function that:

Takes the current tasks array.

POSTs to /api/timeline/save with:

{
  "session_id": <current session id>,
  "scenario_letter": "A",
  "tasks": [...],
  "metadata": { "hours_per_day": 6 }
}


(Include any other fields this endpoint already expects, but do not remove existing ones.)

On success:

setTimelineMetrics(res.metrics);
setResourceRisk(res.metrics?.resource_risk ?? null);


Handle errors gracefully (log and leave current metrics as‑is).

Wrap it in a debounced function so we don’t spam the server:

const saveTimelineDebounced = useMemo(
  () => debounce(async (tasks) => { /* call /api/timeline/save */ }, 400),
  [sessionId]
);


Call it whenever tasks change

Wherever the Gantt component emits changes (e.g. onDateChange, onTaskChange, onUpdateTasks, etc.), make sure we:

Update local tasks state.

Call saveTimelineDebounced(updatedTasks).

The call should happen on mouse‑up / commit events (not every pixel during drag, if the library differentiates).

Bind the header chips to the new metrics

In the header above the Gantt:

TOTAL DURATION → timelineMetrics?.total_duration_days ?? 0 (render as “XXX days”).

TASKS → timelineMetrics?.total_tasks ?? 0.

CRITICAL TASKS → timelineMetrics?.critical_path_count ?? 0.

DEPARTMENTS → timelineMetrics?.departments_count ?? "-".

Bind the Resource Risk Management section

Use resourceRisk for this block:

Risk summary bar:

If !resourceRisk or resourceRisk.items.length === 0, show a neutral message like “No resource idle‑time risks detected yet.”

Otherwise:

const highRiskCount = resourceRisk.summary?.high_risk_count ?? 0;
const totalIdleCost = resourceRisk.summary?.total_idle_cost ?? 0;
const summaryText = `${highRiskCount} high-risk resource conflicts detected. Total potential idle cost: $${totalIdleCost.toLocaleString()}`;


Render that inside the red bar in the design.

Table rows:
Map over resourceRisk.items and show:

Resource/Vendor → row.resource

Waiting Period → ${row.waiting_days} days

Idle Time Cost → $${row.idle_cost.toLocaleString()

Risk Level → row.risk_level (with “High” in the red pill style)

Recommendation → row.recommendation

You can optionally sort items descending by waiting_days or idle_cost so the worst problems are at the top.

Initial load

When Step 4 first loads:

After loading the existing scenario & tasks from /api/scenario/{session_id}, immediately call the same helper (saveTimelineDebounced or a non‑debounced variant) once to populate timelineMetrics + resourceRisk for the baseline schedule.

The end result should be:

Dragging or resizing a bar in Step 4 Gantt → a short delay → the header chips and “Resource Risk Management” panel update automatically using the latest metrics from /api/timeline/save.