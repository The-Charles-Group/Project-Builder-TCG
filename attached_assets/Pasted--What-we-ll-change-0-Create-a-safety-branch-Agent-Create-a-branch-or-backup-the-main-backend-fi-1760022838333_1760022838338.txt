✅ What we’ll change
0) Create a safety branch

(Agent) Create a branch or backup the main backend file before editing (e.g., cp main.py main_before_remove_BC.py).

1) Trim the scenario multipliers to only A

Why: defensive cleanup; nothing should reference B/C after we remove their builders.

Action (edit in the FastAPI backend file):
Find SCENARIO_MULT and reduce it to just A:

SCENARIO_MULT = {
    "A": {"hours_mult": 1.00, "qa_pct": 0.05, "pm_pct": 0.10, "strip_optional": True}
}


(Existing map shows A/B/C today. Remove B and C entries.)

2) Remove Scenario C builder (and any C-only helpers)

Why: hard dependency on a third scenario is no longer needed.

Action:

Delete the whole @app.post("/api/build_scenario_c") def api_build_scenario_c_deprecated(...) function and its body. It references scenario “C” throughout (sorting, _apply_scenario_knobs("C"), labels, etc.).

If you see any other C‑specific helpers that only exist for Scenario C, remove them along with their imports.

3) Narrow Pydantic models to A‑only

Why: front‑end will no longer send/receive Scenario B or C; requests should validate accordingly.

Action (replace class definitions):

Before (you have A and B):
After (only A):

class BuildPayload(BaseModel):
    selected_deliverable_codes: List[str]
    scenario_a: Optional[ScenarioSpec] = None
    pricing_mode: str = "Flat_Blended"
    blended_rate: Optional[float] = None
    rate_band: Optional[str] = "Standard_US"
    use_slack: bool = True
    slack_after_internal: int = 1
    slack_after_client: int = 2
    slack_global_pct: float = 0.05
    project_start: Optional[str] = None
    client_budget_usd: Optional[float] = None
    retainers: Optional[List[RetainerSelection]] = []
    selected_components_map: Optional[Dict[str, Union[str, List[str], Dict[str, Optional[float]]]]] = None
    selected_l3_map: Optional[Dict[str, Dict[str, List[str]]]] = None

class AutoBuildPayload(BaseModel):
    rfp_text: str
    scenario_a: ScenarioSpec
    pricing_mode: str
    blended_rate: Optional[float] = None
    rate_band: Optional[str] = "Standard_US"
    use_slack: bool = True
    slack_after_internal: int = 1
    slack_after_client: int = 2
    slack_global_pct: float = 0.05
    project_start: Optional[str] = None
    client_budget_usd: Optional[float] = None
    retainers: Optional[List[RetainerSelection]] = []


Also delete unused payload classes that only exist for multi‑scenario exports:

ExportWorkbookPayload, ExportWorkbookABCPayload, ExportWorkbookXMLPayload. They take scenario_b/scenario_c and power Excel/XML exports for B/C. We will keep single‑scenario exports via existing /api/export and /api/export_xml.

4) Make /api/build assemble only Scenario A

Why: your current loop builds A and B; we’ll collapse it to A.

What you have today (loop over A and B):

Replace the A/B loop with A‑only logic:

    # Build scenarios
    scenarios = {}

    # Default spec if not provided
    default_spec_a = ScenarioSpec(mode="template", scenario_key="MED_LOW")
    spec_in = (payload.scenario_a or default_spec_a)

    per_deliv = []
    for code in payload.selected_deliverable_codes:
        row = DB.deliverables[DB.deliverables["Deliverable_Code"].astype(str)==str(code)]
        if row.empty:
            print(f"DEBUG Build: No deliverable found for code '{code}'")
            continue

        deliverable_name = str(row["Deliverable"].iloc[0])
        cat = str(row["Category"].iloc[0])

        spec_resolved = _resolve_scenario(spec_in, cat)
        months = int(ret_map.get(code, 0))
        selected_components_dict = comp_map.get(str(code), {})

        out = _scenario_for_deliverable(
            code, cat, spec_resolved,
            pricing_mode, blended_rate, rate_band,
            use_slack, slack_i, slack_c, slack_pct, project_start,
            scenario_letter="A",
            retainer_months=months,
            selected_components=selected_components_dict
        )
        out["deliverable"] = deliverable_name
        out["category"]    = cat
        per_deliv.append(out)

    # Preserve user-locked order if it exists, else sort
    _prev = _current_scenarios().get("A") or {}
    locked = bool(_prev.get("manual_order_locked"))
    if locked:
        built_by_code = {str(it["deliverable_code"]): it for it in per_deliv}
        prev_order = [str(c) for c in (_prev.get("user_order") or [])]
        keep = [c for c in prev_order if c in built_by_code]
        tail = [c for c in built_by_code.keys() if c not in keep]
        ordered_codes = keep + tail
        per_deliv = [built_by_code[c] for c in ordered_codes]

        prev_by_code = {str(it.get("deliverable_code")): it for it in (_prev.get("items") or [])}
        for it in per_deliv:
            code = str(it["deliverable_code"])
            prev_it = prev_by_code.get(code)
            if prev_it and prev_it.get("schedule"):
                sched = prev_it["schedule"]
                it["schedule"] = sched
                it["start"] = sched[0]["start_date"]
                it["end"]   = sched[-1]["end_date"]
                it["duration_days"] = sum(int(r["duration_days"]) for r in sched)
    else:
        per_deliv = _sort_deliverables(per_deliv, "A")

    # Apply scenario knobs and totals
    spec_resolved = _resolve_scenario(spec_in, "General")
    per_deliv = _apply_scenario_knobs(
        per_deliv, "A",
        spec_resolved.get("complexity", "Advanced"),
        spec_resolved.get("tier", "T2_MediumVolume"),
        pricing_mode, rate_band, DB, blended_rate
    )
    price_sum = sum(int(x["price"]) for x in per_deliv)
    hours_sum = sum(int(round(x["total_hours"])) for x in per_deliv)

    scenario_out = {
        "pricing_mode": pricing_mode,
        "rate_band": rate_band,
        "blended_rate": blended_rate,
        "use_slack": use_slack,
        "slack_after_internal": slack_i,
        "slack_after_client": slack_c,
        "slack_global_pct": slack_pct,
        "project_start": project_start,
        "items": per_deliv,
        "totals": {"hours": int(hours_sum), "price": int(price_sum)},
    }

    if locked:
        scenario_out["ai_order"] = list(_prev.get("ai_order") or [it["deliverable_code"] for it in per_deliv])
        scenario_out["user_order"] = ordered_codes
        scenario_out["manual_order_locked"] = True
    else:
        scenario_out["ai_order"] = [it["deliverable_code"] for it in per_deliv]
        scenario_out["user_order"] = list(scenario_out["ai_order"])
        scenario_out["manual_order_locked"] = False

    scenario_out = _inflate_components_if_missing(scenario_out)
    scenarios["A"] = scenario_out

    global _CURRENT_SCENARIOS
    _CURRENT_SCENARIOS.update(scenarios)

    # Optional budget calc (if provided)
    if client_budget_usd and client_budget_usd > 0:
        scenario_price = scenarios["A"]["totals"]["price"]
        budget_delta = scenario_price - client_budget_usd
        coverage_pct = (scenario_price / client_budget_usd) * 100 if client_budget_usd > 0 else 0
        scale_factor = client_budget_usd / scenario_price if scenario_price > 0 else 1.0
        scenarios["A"]["budget_info"] = {
            "client_budget_usd": client_budget_usd,
            "total_price": scenario_price,
            "budget_delta": budget_delta,
            "coverage_pct": round(coverage_pct, 1),
            "scale_factor_if_fit": round(scale_factor, 3)
        }

    return scenarios


(Above mirrors your existing A/B path but only for A. Your current A/B implementation, including preserve‑order logic and knob application, is here for reference. )

5) Make /api/auto_build return only A

What you have today: returns both A and B in a scenarios object.

Action (simplify to A‑only):

Keep _build_for(...) helper.

Build one scenario and return:

scenarios = { "A": _build_for(selected_codes, payload.scenario_a) }
return {"suggested": suggestions, "scenarios": scenarios}


Also remove all references to payload.scenario_b in this endpoint.

6) Remove multi‑scenario export endpoints & payloads

Why: they exist solely to export A+B or A+B+C.

Action:

Delete the endpoint @app.post("/api/export_workbook") def api_export_workbook(...) (it demands A & B and writes two tabs). Keep /api/export (single scenario CSV/XLSX) which already works great.

Delete @app.post("/api/export_workbook_abc") (three‑sheet export).

Delete @app.post("/api/export_workbook_xml") (A&B zipped XML). Keep /api/export_xml (single scenario XML).

After deleting these routes, also remove the now‑unused payload classes you deleted in step 3.

Note: The single‑scenario exports you’re keeping are:

/api/export → CSV/XLSX (1 sheet)

/api/export_xml → single MSPDI XML (1 file)

7) Keep timeline reorder intact (A‑only)

No code change needed—/api/reorder_timeline already defaults to letter “A” and fails cleanly if the chosen letter doesn’t exist. With only “A” in _CURRENT_SCENARIOS, this just works.

🖥️ Front‑end adjustments (vanilla JS)

Minimal changes—your Step 3/4/5 screens should stop referencing Scenario B and only read from scenarios.A.

Build responses: You currently consume {"scenarios": {"A": ..., "B": ...}}. After this backend change it will be {"scenarios": {"A": ...}}.

Remove any UI branches that try to render Scenario B.

Ensure the timeline and summary read data.scenarios.A exclusively.

Export buttons:

Point “Export Workbook” to /api/export (single scenario) instead of /api/export_workbook. Use payload = { scenario: scenarios.A, project_name, file_format: "xlsx", scenario_label: "Scenario A" }—this is already supported.

Point “Export XML” to /api/export_xml with { scenario: scenarios.A, project_name, scenario_label: "Scenario A", sheet_name: "Scenario A" }.

Remove/hide any “A & B” or “ABC” export UI.

Auto‑build: If you call /api/auto_build, stop sending scenario_b and read resp.scenarios.A only.

(If the static JS has strings like “Scenario B” or calls to the removed endpoints, delete them. Search the repo for Scenario B, /api/export_workbook, /api/export_workbook_abc, /api/export_workbook_xml and remove those paths.)

🔎 Quick verifications (smoke test)

Step 3 – Build: Build should return { scenarios: { A: {...} } }, with items and totals populated; no references to B. (Previously, A & B were built in a loop. )

Step 4 – Timeline: Reordering works against Scenario A; save/refresh should preserve order via _CURRENT_SCENARIOS["A"].

Step 5 – Export:

/api/export returns a single CSV/XLSX with the WBS laid out; pricing computed and Price_USD filled.

/api/export_xml returns one MSPDI XML file, using the scenario’s pricing & rates.

No stray endpoints: Hitting deleted routes (/api/export_workbook, /api/export_workbook_abc, /api/export_workbook_xml, /api/build_scenario_c) should 404 (by design).